# 2026-09-23 开发记录

## 统一控制独立审查三项行为修复

- 问题：旧两参位置调用由 VM 与控制会话重复扣费；扩展注册前校验存档会丢失有效恢复结果；战斗输入覆盖朝向控制，到期又恢复过期方向。
- 范围：修复兼容调用扣费所有权、启动加载顺序、Actor 基础/有效朝向分离，补充 VM、启动恢复和战斗输入回归。非目标：调整定价或迁移合同、修改工作流、汇总阶段证据、执行 commit/push/tag。
- 验收：旧调用在仅够旧价格时成功且只扣一次，不足时无位置副作用；扩展加载期间不读写存档，注册后恢复原法术书与配置，加载失败保留原始数据；鼠标和 AI 更新基础朝向，覆写持续有效，到期恢复最新基础值或仍有效下层。
- 实现：旧两参调用复用旧位置实现，VM 保留旧距离价和倍率，三参调用仍由 World 会话收费；`App` 先等待扩展加载再挂载恢复/自动保存组件，加载失败不进入默认书；Actor 增加 `baseAim`，鼠标与 AI 经 `setActorAim` 更新输入，binding 从当前基础朝向合成有效 `aim`，属性重算也刷新朝向层。
- 标准验证：`npm run test -- test/entity-vnext.test.ts test/combat.test.ts test/render.test.tsx test/persistence.test.ts` 退出 1，在加载 Vite 配置时受 `esbuild spawn EPERM` 阻断，未运行断言。
- 定向替代验证：`node .tmp-review-repair-vitest.mjs test/entity-vnext.test.ts test/combat.test.ts test/render.test.tsx test/persistence.test.ts` 退出 0，4 文件 / 89 条通过。临时 runner 使用 `startVitest`、`configFile: false`、`esbuild: false`、单线程 worker、`preserveSymlinks: true` 和 `typescript.transpileModule`，直接转换原测试与源码；未修改正式验证配置，不登记为标准或完整门禁通过。验证后删除临时 runner。
- 静态验证：`npm run typecheck`、`npx eslint src/core/metas/unified.ts src/core/world.ts src/game/battle.ts src/app/App.tsx test/entity-vnext.test.ts test/combat.test.ts test/render.test.tsx --max-warnings 0`、`node scripts/check-docs.mjs`、`git diff --check` 均退出 0；本轮 7 个代码/测试文件与 4 个文档文件的定向 Prettier 写入退出 0。
- 证据边界：只记录本修复的直接验证，不改写 `development.json`/`development.md`，不执行 Feature 的快速/完整门禁、候选体验或 Git 交付；原 Feature PM 仍需复跑标准命令并统一汇总。读取的基线 Git tree 为 `3df89b8aec2bf14970b2e810668c839bc8d4eaf9`，当前工作区另有既存未提交改动。
- 修复代码与测试指纹：`ca9be62aa86ab69667531e3de865533ee8ce83fa528f1439898b53858358a674`。范围为 `src/app/App.tsx`、`src/core/metas/unified.ts`、`src/core/world.ts`、`src/game/battle.ts`、`test/combat.test.ts`、`test/entity-vnext.test.ts`、`test/persistence.test.ts`（只读回归）、`test/render.test.tsx`；按路径排序，以路径、NUL、原始文件内容、NUL 逐项计算 SHA-256。
- 验证配置指纹：`f0edee37f4f2b81b1224175591435abb120cf99d240e405b758b06e0f559d026`；同算法覆盖 `.tmp-review-repair-vitest.mjs`、`package-lock.json`、`package.json`、`tsconfig.json`、`vite.config.ts`，仅标识这次替代执行的配置。
- 最终格式验证：`npx prettier --check src/core/metas/unified.ts src/core/world.ts src/game/battle.ts src/app/App.tsx test/entity-vnext.test.ts test/combat.test.ts test/render.test.tsx docs/reference/entities-and-attributes.md docs/reference/扩展元法术.md docs/status.md docs/dev/2026-09-23.md` 退出 0；文档检查与差异检查均退出 0。

## GPT-6 模型路由更新与正式版本恢复

- 问题：模型家族已更新，而工作流仍以 GPT-5.6 承担常规和核心任务；上一轮正式版本开发在额度错误后保存为可恢复状态。
- 范围：将短时语义判断与 Feature PM 的规划、执行、审查路由更新为 GPT-6 Luna/Sol/Astra；保留旧型号的兼容回退与既有时间、质量和版本门禁。不更改本轮产品策划和工作项。
- 验收：机器可读配置有效，相关路由测试、文档检查和完整门禁通过；从现有开发恢复点续跑，不覆盖已完成 ADR 或进行中的产品改动。
- 依据：OpenAI 模型目录和 GPT-6 迁移指南确认 `gpt-6-luna`、`gpt-6-sol`、`gpt-6-astra` 及现有推理等级可用。上一轮中断日志报告额度不足；本轮先重新核实运行账号与额度，不能把旧错误当作当前状态。
- 恢复实测：首次自然语言续跑被调度器拒绝，因为本次 policy/文档变动不属于产品工作项，但秘书沿用旧额度快照并误报 PM 已确认运行。主 Agent 使用显式 `--takeover` 记录现场后，Feature PM 已用 GPT-6 恢复正式版本开发。控制面补充一次性 `takeoverOnResume` 标记，并要求新恢复快照确认 PM 启动；旧额度快照不再构成新运行证据。
- 已通过：`npm run typecheck`、路由 39 条定向测试、notice guard 43 条定向测试及 `npm run docs:check`。完整门禁暂未登记通过；正式产品版本开发仍在执行，最终由 Feature PM 在完整代码树上统一验证。

## 正式版本开发证据一次性汇总

- 问题：11 个正式工作项已有执行 Agent 输出，旧开发报告仍只覆盖 ADR，状态页也停留在单项阶段，容易把计划命令或未启动的测试误认为通过。
- 范围：按任务清单及前序输出写入逐项 `development.json` 和公开报告，并同步状态页；保留现场中的产品实现和其他工作流改动。非目标：修复产品代码、执行 Feature 快速/完整门禁、推进版本节点或 Git 交付。
- 验收：JSON 覆盖全部 11 个正式 ID，按依赖顺序记录实际直接命令及退出码；失败的 Vitest、参考生成和未解决的四步链等价性明示为待复核；文档校验通过。
- 结论：11 项已有输出，但开发阶段不能判定通过。多数 Vitest 与跨层 Playwright 因 `spawn EPERM` 未执行断言；参考生成也未运行成功。`dsl-compat-migration` 的四步弹道链失败路径等价性仍未验收。Feature PM 需在可运行环境闭环并执行其作用域门禁；本报告不代填这些结果。

## Feature 快速门禁格式修复

- 问题：独立审查报告 `npm run format:check` 在 `src/core/analyzer.ts` 失败。
- 范围：仅按仓库 Prettier 配置格式化该文件并记录直接验证；非目标：调整分析器行为、覆盖既有产品改动、汇总阶段证据或进行 Git 交付。纯格式修复不改变产品能力与架构，无需新增行为测试或更新长期能力说明。
- 验收：失败命令 `npm run format:check` 通过，分析器仍通过类型检查；后续快速门禁和最终完整门禁由原 Feature PM 继续执行。
- 已执行：`npx prettier --write src/core/analyzer.ts`、`npm run format:check`、`git diff --check` 均退出 0。额外尝试的 TypeScript printer 输出哈希比较断言失败（Node 退出 1）；printer 保留部分源码布局信息，该比较不能作为语法等价性证据，因此不登记等价性通过，改用仓库类型检查复核。
- 类型复核：`npm run typecheck` 退出 0。格式问题已修复，未新增行为测试，未执行 commit、push 或 tag，未改写阶段汇总证据。

## Feature 快速门禁测试修复

- 问题：原 PM 的 `fast-verify-4.log` 中 `npm run test` 有四项失败：渲染用例共享旧存档导致两个默认法术入口消失，持续护体测试仍检查旧 `mods`，一参创建的正则把五参创建中的嵌套调用误判为旧接口。
- 范围：修复 `render`、`combat`、`persistence` 三组测试的隔离与语义断言；非目标：改变已批准的游戏行为、调度工作项、汇总阶段证据或进行 Git 交付。
- 验收：四项原失败可复现，修复后这三组测试全部通过；周期施法仍验证重复执行、控制记录和实际护体效果，默认与示例 DSL 都通过 AST 编译检查创建调用为五参，默认法术书仍验证安全迁移。示例书中的递归法术是有意保留的错误示范，不要求整本示例通过迁移分析。标准门禁若受环境阻断则如实记录，由原 Feature PM 继续。
- 标准命令：`npm run test` 退出 1，加载 Vite 配置时 `esbuild spawn EPERM`，尚未执行断言。临时 runner 使用 `startVitest`、`configFile: false`、`esbuild: false`、线程池及 `typescript.transpileModule` 转换原 TS/TSX；Windows 下显式保留符号链接解析以免触发 Vite 的 realpath 子进程探测。不修改正式配置或测试行为，也不作为标准门禁证据。
- 复现：`node .tmp-repair-vitest.mjs test/render.test.tsx` 退出 1（2 失败 / 4 通过），`node .tmp-repair-vitest.mjs test/combat.test.ts test/persistence.test.ts` 退出 1（2 失败 / 38 通过），与原 PM 四项失败一致。中途把整本示例也要求迁移成功的验证退出 1，暴露其有意保留的递归错误示范，已改用编译后的创建实参检查，保留默认书迁移校验。
- 修复后：`node .tmp-repair-vitest.mjs test/render.test.tsx test/combat.test.ts test/persistence.test.ts` 退出 0，3 文件 / 46 条全部通过；`npm run typecheck`、`npx eslint test/render.test.tsx test/combat.test.ts test/persistence.test.ts --max-warnings 0`、`git diff --check` 均退出 0。仅修改测试与文档；临时 runner 与日志验证后清理，未执行 commit、push 或 tag，未改写正式版本阶段汇总。
- 文档与格式：`npx prettier --check test/render.test.tsx test/combat.test.ts test/persistence.test.ts docs/testing.md docs/status.md docs/dev/2026-09-23.md`、`node scripts/check-docs.mjs` 均退出 0。原 Feature PM 仍需继续标准 `npm run test` 及后续门禁。
