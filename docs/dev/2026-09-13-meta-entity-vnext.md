# 2026-09-13：元法术与统一实体纵向切片续作

## 问题、范围与验收

接续上次 delivery 超过 720 秒后留在当前工作区的改动，目标是使“创建弹道 → 配置 → 激活 → 探查 → 实战命中”可运行，并把关键设计决策拆入 ADR 0009、0010 与功能规格。保留旧法术兼容、AST 唯一 IR 和纯 Node 核心；不扩展通用 ECS、不移除冷却、不新增其他实体原型。验收包括成功链路、失败不产生部分修改、资源上界及旧玩法回归。

## 本轮修复

- 延续已有统一 ID、实体能力、动态价格与默认法术实现，未重做项目规划。
- 修复 `World.reset()` 重用旧句柄；移除和重置后的旧句柄不能误指向新对象。
- 拒绝零向量和非有限方向，非法寿命/速度/威力返回空或 false；非有限参数不会先使动态价格变成 NaN。
- 旧 `发射` 与新创建共享每所有者 16 枚上限，创建失败不再产生虚假的发射特效。
- 把探查动态价从原稿的 `min(12, 2 + 距离 / 200)` 调为 `min(12, 8 + 距离 / 200)`，保留既有多人场景“快照用神识换法力”的优化关系；这由现有核心测试保护。
- 补齐目录、详情和定价表的动态上界展示，以及实体参考、生成元法术参考、状态和规格。纠正原稿提前标为“已验收”的状态。

## 验证与环境限制

- 聚焦核心、战斗和实体边界测试 49 项通过；包含非法价格拒绝、倍率后静态上界、权限、数量上限、未激活寿命、重置及旧发射兼容。
- jsdom 两项通过，实际调用 App 的目录/详情选择和默认法术推演；现有路由测试 11 项通过。合计 62 项在临时配置下通过（界面选择器修正后重跑界面两项）。
- 标准测试启动受 `spawn EPERM` 阻塞；临时使用 Vitest Node API，关闭配置文件打包，TypeScript 内存转译并使用单线程池和 preserveSymlinks。没有更改正式 Vite、Vitest 或门禁配置，临时辅助文件已清理。
- `npm run verify` 执行到 docs:check：类型、lint、格式通过，tsx/esbuild 启动子进程时被环境拒绝。相同参考生成器通过临时 Node 转译钩子完成生成与一致性检查（65 个元法术）；文档链接检查通过。
- 新增 E2E 用例覆盖目录上界、默认新法术推演、页签往返、760px 窄屏。聚焦 E2E 命令在浏览器启动前被 `spawn EPERM` 阻塞；未声称真实浏览器或视觉验收通过。

## 秘书交接

入口：推演台选择“御剑·手动”并推演；演武场绑定到槽位后朝妖兽发射。浏览器体验需检查 1280×800 与 760×800，确认飞行、命中、未激活不渲染、目录可达性和往返状态。

按任务明确约束，本轮未创建其他 Agent，未执行 verify:full，未 commit/push/tag 或发布。已自查相关差异，但不把自查称为独立审查；秘书仍需在允许子进程的环境完成标准 verify、完整门禁、真实体验和独立审查。

## 秘书收束

- `npm run verify:full` 已通过：62 个 Vitest、12 个 Playwright E2E、覆盖率、沙盒与生产构建全部成功。
- 浏览器实际体验通过：推演台动态价格与“御剑·手动”推演正常；演武场绑定数字键后可施放统一句柄弹道，控制台无错误。
- 修复沙盒结论仍硬编码旧固定法力的问题，表格与结论改为格式化并引用当次实测数据。
- Sol 独立审查因模型满载返回基础设施错误；秘书已完成差异复核，调度器另行补充审查模型重试、备用路由和恢复点。
