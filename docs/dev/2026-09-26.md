# 2026-09-26 上版游戏方向的重新解读

## 问题与边界

制作人指出试运行修订 1 错把 Agent 协作流程当成游戏版本主题。真正要验证的是：主策能否用新的解读方式重读上一正式游戏版本中制作人的原始方向，识别旧策划是否将举例当作封闭清单，再提出一项小规模、可玩且待制作人确认的修正。旧版归档与代理验收事实保留，不追改为未交付；本轮不自动批准新策划或启动开发。

## 本轮处理

- 使用正式生命周期 API 记录制作人对修订 1 的 `changes-requested`，建立范围修订 2，并回到 `charter-draft`。旧修订仍在 Git 和版本记录中。
- 以已归档版本状态的制作人 `direction` 和 `scopeRevisions` 为一手信号，旧策划、详细策划、参考文档及代码只作旧解读与交付证据。
- 主 Agent 暂拟修订 2 的[版本策划草案](../versions/pilot-agent-intent-2026-09-24/charter-draft.md)和[意图记录](../versions/pilot-agent-intent-2026-09-24/intent-alignment.json)。假说是：旧版实现了资源账本和固定法球行为，但仍需核查玩家是否能将自定义行为法术装载给法球并按授权额度运行。候选切片、账户方式和原则本身都保留待主策与制作人核对。
- 主策规划 PM 三次调用遭遇相同 `workspace routing discovery failed`，没有生成新策划任务清单；主 Agent 停止 notice guard 以阻止无进展重试，保留运行目录 `.daoyan-agent/runs/secretary-formal-pilot-agent-intent-2026-09-24-2-charter-draft-1`。此为外部执行阻断，不是游戏实现缺陷。

## 验证与后续

`intent-alignment.json` 对版本 ID 和策划修订 2 的解析通过，三个相邻情形符合结构合同；`git diff --check` 通过。格式检查发现 Markdown 表格问题后已格式化，仍需复查。没有运行产品完整门禁，也没有触发正式版本策划交付或制作人批准。

连接恢复后，从保存的规划恢复点继续，正式主策应独立检查原话、旧交付和代码证据；独立审查再判断推断是否合理。待 Version PM 收束并进入 `charter-review` 后请制作人判断原则和小切片。只有该评审通过才进入详细策划和开发。

## 制作人再次纠正：统一世界规则

制作人指出修订 2 仍把整体问题缩成“法球碰撞响应”单点：真正的缺口是修士、法球及未来实体没有同一套可解释的创建、事件、施法和资源规则。碰撞伤害应为事件触发伤害法术；法球施法资源来自创建者注入，不能自行恢复。运动、爆炸、伤害和算法优化都要有因果、价格与账本。可组合的追踪不应占据元法术名额；还须补充有代表性的预设并同步敌方修士。

- 已中断按旧解释运行的 Feature PM 续跑，确认没有仍在运行的同名进程；notice guard 保持暂停。
- 使用正式生命周期 API 将方向并入未冻结草案的范围修订 3，保留修订 1、2 及旧 PM 运行记录。旧任务清单仍标范围修订 2，不能作为新方向的有效执行合同。
- 主 Agent 改写版本策划与意图记录，先明确共享原则、对照代码特例、推导相邻情形，再推荐小范围但贯通共享管线的闭环。旧 `Projectile.behavior` 枚举、`设置法球追踪` 元法术、`hitProjectile` 固有伤害分支及 Actor 专属事件 VM 均是复核线索，不以文档推断冒充最终缺陷定论。
- 本次未改游戏代码，未进入详细策划或开发。待格式、文档合同与旧运行状态检查后，正式主策须在范围修订 3 重新规划并评审；制作人立项评审仍是开发前门禁。

定向验证：`parseProductIntentAlignment` 对版本 ID、策划修订和四个相邻情形解析通过；修改文件的 Prettier、`docs:check` 和 `git diff --check` 均通过。`npm run verify:full` 已尝试，类型检查与 lint 通过，但全仓 `format:check` 因未改动的 `work/feature-pm-flow-comparison.html` 格式问题退出 1，未进入测试、覆盖率、沙盒、E2E 或构建。此草案不登记完整门禁通过，也不据此宣称游戏交付。

`work/` 是本机临时产物目录，不在 Git 管理中；Prettier 之前仍扫描它，导致版本策划草案无法通过仓库 pre-commit 门禁。现将该目录加入 `.prettierignore`，不修改该 HTML 本身，再重新运行完整门禁。

修正扫描范围后，`npm run verify:full` 退出 0：类型、lint、格式、文档检查通过；标准测试 492 条、覆盖率测试 492 条、浏览器 E2E 25 条通过，沙盒和生产构建通过。此门禁仅证明当前策划与仓库状态未破坏既有行为，不表示新法术框架已实现或候选体验通过。
