# 2026-09-21 开发记录

## 交付门禁修复：被制作人阻塞的过期重试保持休眠

- 问题与范围：独立交付审查发现，当 `waiting-producer`（或仍占用写进程的 `tracking`）项目排在已到期 `retry-wait` 前时，notice guard 虽正确阻止恢复派发，却会在每次纯对账时无条件刷新工作项/全局时间戳并写回状态。仅修复该无语义变化的写循环与其直接回归；不改变 `.daoyan-agent/` 现场、版本节点或派发策略。
- 修复：`reconcileItem` 不再在读取快照时预先改写 `item.updatedAt`；`coordinateOnce` 在规范化后比较对账前后的状态，只在迁移规范化或对账确有语义变化时写入。已确认的过期重试仍由阻塞项的外部事件（制作人决定、写进程退出等）重新唤醒，不能自行制造轮询事件。
- 验收与验证：`secretary-dashboard` 的 HTTP 回归覆盖 `waiting-producer` 与 `tracking` 两种阻塞者，连续空闲窗口内 `state.json` 应保持字节不变，后续外部事件仍按原场景继续处理。`npx tsc --noEmit`、对修改脚本的 ESLint、Prettier 检查、`npm run docs:check` 和 `git diff --check` 均通过。标准聚焦 Vitest 在加载 `vite.config.ts` 时被环境的 `esbuild spawn EPERM` 阻断，未执行用例；无配置预编译尝试同样无法创建该 HTTP 场景所需的秘书子进程，不能记为通过。

## 自适应项目中枢：详细策划

- 输入与范围：仅完成正式版本 `adaptive-project-office-2026-09-21` 的 `module-design` 阶段；先核对版本策划、内核规格、ADR-0016、工作流、直接实现和直接测试。不创建 Agent、不改 `.daoyan-agent/` 运行状态、不推进版本节点、不 commit、push、tag 或发布。
- 问题与结论：把既有工作台切片和自适应编排内核收束为四个可审核模块：可组合工作台、公开 Agent 活动投影、正式版本自适应编排、阶段证据与独立 QA/缺陷闭环。状态所有权、事件驱动边界和 v1 兼容策略均复用已采纳 ADR-0016；未发现需要制作人决定的新产品冲突或不可逆架构取舍。
- 交付：新增 `docs/versions/adaptive-project-office-2026-09-21/module-design.md`，逐项定义桌面/窄屏交互、空/错状态、公开数据白名单、方向去向、风险与阶段策略、全队列对账、阶段产物和质量门禁；明确本阶段不预造后续 JSON 证据，也不执行生产迁移、降级演练、候选体验或发布。`docs/status.md` 同步当前阶段事实和未完成边界。
- 验收与验证：本文的勾选核对覆盖规则、交互、边界和验收标准。直接相关的三份 Vitest 测试在加载 `vite.config.ts` 时受环境 `esbuild spawn EPERM` 阻断，未开始执行用例；`npm run verify` 已运行，typecheck、lint、format:check 和 docs:check 均通过（66 个元法术、98 个 Markdown 文件），其 Vitest 阶段受到同一阻断，整条 verify 不记为通过。未运行 `verify:full`，由秘书统一收束。

## 独立审查修复：等待快照确认、承诺变化与代码修订 QA

- 输入/问题：制作人恢复操作被旧等待快照撤销；运行/候选词面查重吞掉相反方向；修复后的代码沿用修复前 QA，均为交付前 high 问题。
- 范围与非目标：仅修秘书对账/收件、候选质量契约及直接测试文档；保留已有工作区改动与 v1 数据，不操作生产状态，不创建 Agent，不 commit、push 或 tag，不代表正式版本验收或发布。
- 验收：真实等待快照经回复或看板决定后，重启仍能从原恢复点带指导派发；变化后的阻塞再次等待。有运行/候选事实的取消、禁止、替换、扩展方向不能被去重；冻结前走范围评审，冻结后走候选。A 修订 QA 通过后修复为 B，execute/reduced/skip 均拒绝无新回归的候选，修订标签相同也不能沿用旧结果。
- 公开计划与进度：核对现场与直接契约 → 修复并补单元/HTTP 场景 → 检查兼容与重启行为 → 同步 ADR/长期/版本文档 → 执行相关门禁。时间：2026-09-21；token：当前环境未提供可审计用量，记为不可用。
- 动作与文件：`secretary-state.ts` 增加快照确认及方向同文查重；`secretary-notice-guard.ts` 保留已确认等待的重试，确认旧记录前补对账，新阻塞和写进程仍阻断；`version-lifecycle.ts` 记录当前代码与 QA 失效位置。对应状态、生命周期及 HTTP 看板测试和 ADR-0016、工作流、规格、状态、版本 charter 同步。
- 兼容决策：v1 可选 `waitingSnapshot`/`acknowledgedWaitingSnapshot` 与 `codeRevision`/`qaInvalidatedThrough`；缺失不补造历史事实，损坏值拒绝。旧修复修订与 QA 冲突保守阻止候选，历史通过记录保留。停写备份须包含确认标记及原 QA 数组，旧写者仍禁止混跑。
- 无头回归：最终将四个实际测试文件以 `tsc --strict` 预编译到忽略目录 `node_modules/.cache/daoyan-review-resume/`，按本日志既有无配置单线程 Vitest 方法执行，86 条通过（生命周期 45、秘书状态 28、守卫工具 10、公开日志 3）。旧状态、损坏新增字段、修订不符、同标签修复、同文去重及相反承诺均有覆盖。
- 恢复补充验证：在上述隔离缓存副本导出实际 `coordinateOnce` 和待办处理入口，模拟进程归属为空且关闭派发，5 个恢复场景通过：产品等待回复/看板指导、账号等待回复/立即重试/定时重试；验证真实快照读取、JSON 持久化/重新规范化、重新对账保留可派发项与指导参数，同轮次变化后的阻塞重新等待。该冒烟未模拟模型，不接触生产状态；它没有实际启动 Feature PM，不能替代 HTTP/子进程集成。
- 环境阻塞：标准聚焦测试及 `npm run verify` 在 Vite 配置加载时因 esbuild `spawn EPERM` 未开始用例；快速门禁此前的 typecheck、lint、format:check、docs:check 通过。额外用 TypeScript 内存转译避开配置编译后，新增/加强的 7 条 HTTP 用例均在启动守卫子进程时遇到 `spawn EPERM`，未进入场景断言，不记为通过。
- 完整门禁：最终 `npm run verify:full` 已执行，typecheck、lint、format:check、docs:check 全部通过（66 个元法术、97 个 Markdown 文件）；Vitest 配置加载再次因 `spawn EPERM` 退出 1，后续覆盖率、沙箱、E2E、构建未执行，因此完整门禁未通过。`git diff --check` 通过。生产备份/迁移、允许子进程环境下的 HTTP 回归和最终制作人体验仍由秘书收束。本次没有 commit、push 或 tag。

## 独立审查修复：等待分流、到期休眠与候选 QA

- 第二轮复审：修复“新增审批确认功能”因包含“确认”而误批准旧门禁，以及同一工作项后续自测失败仍被历史通过掩盖。决策解析先排除明确新方向；开发门禁只采信当前范围最后一条 Feature 自测。聚焦执行守卫工具、版本生命周期和真实 HTTP 看板测试共 50 条通过。
- 最终复审：首次修正曾因粗粒度 `intent=direction` 误吞“这里有问题，需要调整范围”；改为只对明确新方向提前返回，并补真实 HTTP 连续场景：含审批词的新方向保持旧门禁 open，明确退回使其 rejected。独立 Astra 只读复核 verdict=pass，无 findings。
- 输入/问题：独立审查发现等待事项吞掉新方向、被阻塞的到期重试零延时循环、正常修复策略放行失败回归三项 high 问题。
- 范围与非目标：仅修改秘书意图/协调器、候选入口及直接测试和文档；不新增持久字段、不改 v1 迁移格式、不操作生产状态，不执行 commit、push 或 tag。
- 验收：等待上下文中的新方向独立进入版本路由且旧指导不变；到期重试被待决事项或存活写进程阻塞时保持休眠，解除阻塞事件重新对账；execute/reduced/skip 均拒绝失败的最新回归，无缺陷也不例外，新的可信通过结论后才放行。
- 实现：共用明确方向识别，等待上下文恢复语义判断；重试调度集中在全队列对账后的未阻塞分支，按队首安排并清除陈旧计时器，接管新发现运行后重新协调；候选入口统一要求当前范围、独立身份、成功命令与公开证据及回归套件。已有状态无需新迁移。
- 测试：本地意图与旧事项不变断言、三种策略下先通过再失败回归的门禁及重新通过；新增真实 HTTP 的等待/占用休眠、等待期间新方向候选路由与待办解除回归。
- 聚焦验证：标准 Vitest 在加载 Vite 配置时遇到 esbuild `spawn EPERM`，未执行用例。将四个实际测试文件预编译到 `node_modules/.cache/daoyan-review-behavior/`，按本文已有 `startVitest` 无配置单线程方式运行，59 条通过（生命周期 33、秘书状态 14、守卫工具 9、公开日志 3）。这不包括真实 HTTP/子进程路径，不替代完整门禁或制作人体验。
- 修复 Agent 隔离环境：其 `npm run verify:full` 在 Vitest 配置加载时受 `spawn EPERM` 阻止，因此该子 Agent 未把完整门禁记为通过。外层秘书随后在正常权限环境完成真实 HTTP 回归和完整门禁。
- 最终门禁：`npm run verify:full` 通过，17 个测试文件 173 条全部通过；覆盖率 85.61% statements / 80.8% branches，资源沙箱通过，19 条 Playwright E2E 通过，生产构建通过。未操作生产迁移或宣称制作人候选体验已完成。

## 强制接管收束：QA 身份与公开日志并发

- 接管范围：保留并复核已有自适应编排、迁移、对账、看板和文档改动；只修正本目标直接边界，不创建 Agent、不操作生产 `.daoyan-agent/` 状态、不提交或发布。
- 审查发现：独立 QA 原先只在“同 Agent 且同代码修订”时拒绝，Feature Agent 更换修订后可绕过身份隔离；公开 JSONL 采用读后追加，并发写入可能分配相同序号或重复落盘同一事件。
- 实现：Feature 自测新记录必须关联真实工作项、执行身份、代码修订和公开证据；QA 身份按整个版本与 Feature 验证身份隔离，不受代码修订变化影响，新通过结论必须包含至少一条退出码为 0 的命令且有公开证据。旧的不完整记录保持可读，但不能通过阶段或缺陷关闭门禁。同一路径的公开事件追加使用进程内队列串行执行，保持 eventId 幂等和单调序号。
- 测试：新增换修订身份绕过、失败命令冒充通过、旧不完整 QA 可读但不受信、12 路并发追加与重复事件回归。标准五文件 Vitest 在加载 Vite 配置时仍因 `esbuild spawn EPERM` 未开始用例；将四个无头逻辑测试文件预编译后以无配置单线程 Vitest 执行，生命周期 30、秘书状态 14、守卫工具 9、公开日志 3，共 56 条通过。真实 HTTP/子进程 6 条在同一环境因 `spawn EPERM` 无法执行，不记为通过。
- 门禁与风险：已执行 `npm run verify`；typecheck、lint、format:check、docs:check 全部通过（66 个元法术、97 个 Markdown 文件），最后的标准 Vitest 仍在加载 `vite.config.ts` 时因 `esbuild spawn EPERM` 未开始测试，因此整条 verify 不能记为通过。`git diff --check` 通过。生产备份/迁移/保留增量降级演练、跨进程文件锁、允许子进程环境中的 HTTP 回归和最终制作人体验仍由秘书统一收束。

## 架构边界复核：读取副作用与跨状态恢复

- 输入与范围：仅承接“确定架构决策与迁移边界”，在现有工作区交付文档，不创建 Agent、不操作生产状态、不提交或发布。先核对工作流、不变量、ADR-0012/0013/0014 和直接相关代码/测试。
- 现场分类：ADR-0016、规格、架构总览、状态、版本策划和日志为直接产物；dispatcher、notice guard、秘书/版本状态、公开日志及其测试和 agent workflow 为总体目标的相关现场实现，只读核对并完整保留；没有清空现场或修改运行代码。
- 问题与验收：读取正式版本可能先恢复事务并写盘，旧文档容易把读取当成迁移预览；需要明确状态所有权、原子写入的实际范围、中断后去向、回退前提和未证明的边界，不引入新产品语义或重复批准。
- 公开决策：维持已采纳 ADR-0016 和九条架构不变量；补清先备份原字节、隔离预览、normalizer 使用副本、current 缺失时的种子来源、跨状态请求重放与通知至少一次投递。规格列出六类中断位置；完整旧字段/跨文件引用校验和保留增量的降级工具仍属生产放行条件，不能由已有嵌套校验推定完成。
- 文件：更新 ADR-0016、内核规格、架构总览、当前状态、版本策划和本日志；保留先前验证记录，明确设计验收与版本验收不同。
- 聚焦验证：标准四文件命令在加载 Vite 配置时因 esbuild `spawn EPERM` 失败，未开始用例。用 `tsc` 将实际测试及依赖预编译到忽略目录 `node_modules/.cache/daoyan-architecture-boundaries/`，再用 Vitest Node API 关闭配置加载/esbuild、单线程执行，4 文件 54 条全部通过（生命周期 29、秘书状态 14、守卫工具 9、公开日志 2）。补充验证没有改变项目配置，不代替标准门禁、真实 HTTP 或生产演练。
- 快速门禁：已执行 `npm run verify`；typecheck、lint、format:check、docs:check 全部通过（66 个元法术、97 个 Markdown 文件），测试阶段再次在配置加载时因 `spawn EPERM` 退出，整条 verify 未通过。`git diff --check` 通过。未执行 `verify:full`。
- 剩余风险：跨状态全提交点故障注入、生产备份/升级/降级演练、独立审查、完整门禁和最终体验由秘书收束；本轮仅自查文档与直接代码、测试的对应关系。

补充聚焦验证复现：使用本日志下文的 `startVitest` 无配置启动方式，将缓存目录换成 `daoyan-architecture-boundaries`，include 设为 `node_modules/.cache/daoyan-architecture-boundaries/test/*.test.js`；预编译命令如下：

```powershell
node node_modules/typescript/bin/tsc --noEmit false --module ESNext --moduleResolution bundler --target ES2022 --esModuleInterop --skipLibCheck --types node --rootDir . --outDir node_modules/.cache/daoyan-architecture-boundaries test/version-lifecycle.test.ts test/secretary-state.test.ts test/secretary-notice-guard.test.ts test/public-work-log.test.ts
```

## 独立复审收束：决策重放、重新审批与实时活动

- 问题：制作人决策在版本写入后、回复写入前中断时可能被重放到下一个门禁；关键决策退回后没有实际重新审批入口；首次公开门禁事件会遮蔽后续实时修复进度。复核时还发现短确认词可能进入语义立项，以及终态快照只检查 PM 而遗漏存活子 Agent。
- 实现：门禁新增可选 `resolutionHistory`，按请求 ID 保存决议并供收件恢复去重；不可逆/升级策划退回后生成关联原门禁的重新审批待办。看板合并白名单事件与当前心跳、恢复点及脱敏输出。无等待上下文的“好的/收到/ok”等短回复直接保留为回复，不调用模型立项；终态对账同时检查 PM 与子 Agent 进程身份。
- 验证：聚焦运行 `test/version-lifecycle.test.ts`、`test/secretary-state.test.ts`、`test/secretary-dashboard.test.ts`，49 条通过。覆盖真实 HTTP 决策操作、版本已写但回复未写的重启窗口、下一个门禁保持 open、拒绝后重新审批、终态子 Agent 占用与公开事件/实时心跳并存。最终完整门禁和独立复审由秘书在本轮收束末尾记录。
- 非目标：仍不迁移生产 `.daoyan-agent/` 数据，不执行在线降级，不替制作人完成候选版本体验或大版本发布。

## 独立审查修复：拒绝门禁、决策待办与复验轮次

- 问题：被拒绝的不可逆决策可以通过阶段推进绕过；看板决策待办被误当作阶段评审；重新打开的缺陷可复用旧复验证据关闭。
- 范围：修正生命周期与看板直接契约，补充拒绝、迁移、HTTP 操作及缺陷重开回归；保留现有 v1 信封和原有未提交改动。
- 非目标：不扩展已批准产品语义，不操作生产状态，不执行 commit、push 或 tag，也不替代正式版本的最终验收。
- 验收：不可逆/升级策划拒绝后保持阻断，只有该门禁获得有效批准才放行；决策待办按 ID 关闭对应门禁，不消费阶段批准或其他门禁；复验同时匹配当前缺陷、修复轮次、代码修订和范围修订，旧证据不能用于新修复。
- 实现：待办新增可选 `decisionGateId`，旧待办按阶段、摘要、创建时间进行唯一匹配迁移，歧义停止写入；缺陷进入 fixing 生成 `fixAttemptId`，提交 verify 明确 `fixCodeRevision`，QA 的 `bugFixes` 保存缺陷及轮次绑定。旧已关闭历史不改写，旧未绑定证据不补造可信度，待复验缺陷需重新提交修复与测试。
- 验证：`npm test -- test/version-lifecycle.test.ts test/secretary-dashboard.test.ts test/secretary-notice-guard.test.ts` 与 `npm run verify:full` 在加载 Vite 配置时被 `spawn EPERM` 阻止。完整门禁此前的类型、lint、格式和文档检查通过，后续覆盖率、沙盒、E2E、构建未执行。通过下述预编译方式实际执行全部 28 条生命周期测试并通过，包括看板共用的 `applyVersionTodoDecision` 决策分流、拒绝后阻断、旧待办迁移、阶段评审隔离、缺陷重开及代码修订失效。HTTP 回归已增加非评审/评审阶段的通过与退回矩阵，但真实子进程不能启动，未将 HTTP 或实际看板体验记为通过。未改项目测试配置，临时产物只在忽略的缓存目录；`git diff --check` 通过。
- 剩余：生产升级/降级演练与最终制作人体验仍由秘书统一收束；本轮未宣称完整门禁或正式版本交付完成。

本轮环境下的生命周期验证复现方式（PowerShell）：

```powershell
node node_modules/typescript/bin/tsc --noEmit false --module ESNext --moduleResolution bundler --target ES2022 --esModuleInterop --skipLibCheck --types node --rootDir . --outDir node_modules/.cache/daoyan-review-fixes test/version-lifecycle.test.ts
@'
import { startVitest } from 'vitest/node';
const ctx = await startVitest('test', [], {
  config: false,
  include: ['node_modules/.cache/daoyan-review-fixes/test/version-lifecycle.test.js'],
  exclude: [], pool: 'threads',
  poolOptions: { threads: { singleThread: true } },
  watch: false, reporters: ['default'],
  server: { deps: { inline: [/daoyan-review-fixes/] } },
}, { esbuild: false, resolve: { preserveSymlinks: true } });
await ctx?.close();
'@ | node --input-type=module
```

## 架构任务强制接管复核

- 输入：只承接“确定架构决策与迁移边界”，不创建 Agent、不操作生产状态、不提交或发布；完整门禁与独立审查由秘书统一执行。
- 现场分类：已有 ADR、规格、架构总览、状态、版本策划和本日志属于本任务直接产物；`secretary-state`、notice guard、version lifecycle、dispatcher、公开日志及其测试、agent workflow 属于总体目标的相关实现，保留并只读核对；既有界面切片不在本次修改范围。未清空或覆盖现场，未新增运行代码变更。
- 问题/范围/非目标：ADR 中仍有“只对账活动项”“normalizer 只修复归档”的过时陈述；本轮修正事实，补齐字段迁移映射、兼容落地矩阵、停写与禁混跑约束、回退判定及演练证据，不扩大内核范围。
- 决策：维持已采纳 ADR-0016 和现有架构不变量。旧范围基线的 approved 不等于制作人批准；`NO_DISPATCH` 不等于只读。待恢复事务目前未在写盘前检查扩展，normalizer 仅检查主要容器；将事务预校验、嵌套数据校验、跨状态提交中断重放、保留增量的降级工具/演练列为生产迁移放行条件。
- 文件：更新 ADR-0016、内核规格、架构总览、状态、当前版本策划案与本日志；保留早先执行记录，不将设计合同或已有测试断言记为生产验收通过。
- 验证：本轮四个直接相关测试文件的聚焦命令和 `npm run verify` 均在加载 Vite 配置、启动 esbuild 时遭遇 `spawn EPERM`，未开始执行测试。快速门禁中的 typecheck、lint、format:check、docs:check 均通过（66 个元法术、97 个 Markdown 文件）；不能把整条 verify 记为通过。未运行 `verify:full`。
- 审查与剩余：已自查文档与实现/测试的直接映射；未执行独立审查、生产升级/降级或最终体验。完整版本 Definition of Done 仍由秘书收束。

## 独立审查修复：阶段错位与退出对账

- 问题：交付门禁报告 7 项失败；阶段枚举重复插入 `design-review`，导致相邻推进、审批和归档索引错位；全队列对账把历史待办重新选为活动运行，且子 Agent 退出后仍可能被旧运行快照恢复为占用。
- 范围：恢复原有 13 阶段顺序；活动指针优先表示工作区占用，保留当前等待回复上下文但不提拔无关历史待办，待制作人事项继续独立阻止派发；已清空 PM 引用的跟踪项仍利用恢复点中的进程身份确认退出，并清除子 Agent 占用标记。
- 非目标：不改变批准语义或阶段 ID，不操作生产状态、不发布、不执行 commit、push 或 tag。
- 验收：完整相邻阶段流程经过审批与独立 QA 后归档到 100%；无批准推进保持原状态；旧状态迁移生成 13 项策略且重复迁移无变化；存活子 Agent 优先显示为活动项，退出后释放指针且保留待办。
- 验证：定向 Vitest 与 `npm run verify:full` 均在加载 Vite 配置、启动 esbuild 时被环境 `spawn EPERM` 阻止，未开始用例。完整门禁此前的类型检查、lint、格式、文档检查通过（66 个元法术、97 个 Markdown 文件）；后续覆盖率、沙盒、E2E、构建未执行。使用 TypeScript 将实际生命周期模块编译到临时目录后执行 Node 断言冒烟，已通过 13 阶段顺序、旧状态保守幂等迁移、审批拒绝不变更状态、独立 QA 门禁和 100% 原子归档。HTTP/真实子进程对账回归已补断言，但仍须在允许子进程的环境执行；本次未宣称 7 项失败已通过复验或版本已交付。

## 自适应版本编排：架构决策与迁移边界

- 问题：自然语言去向、阶段风险、跨运行恢复与独立验收缺少统一合同；旧待审运行不等于正式版本验收。
- 范围：仅核对直接相关实现/测试与 ADR-0012/0013/0014，形成 [ADR-0016](../adr/0016-自适应版本编排与兼容迁移边界.md) 和[内核规格](../specs/2026-09-21-adaptive-project-office-kernel.md)，同步架构总览、状态和版本策划案。
- 非目标：不改运行代码或现场状态，不扩展项目方向，不创建其他 Agent，不进行 commit、push、tag 或发布。
- 结论：保留 v1 信封与原有状态所有权，以版本化可选扩展承载策略与审计；缺失历史证据保守处理；启动/恢复前全队列对账；迁移可中断重放，回退必须保留新增事实，不能恢复陈旧重试。固定制作人门禁不变，测试与公开日志分别有独立证据合同。
- 核对：现有生命周期已有相邻推进、批准修订校验和写事务；guard 当前主要对账 activeItemId，review-ready 投影为队列 delivered；这些事实在规格中明确为后续实现边界，没有将其误报为新功能已完成。
- 验证：已运行三个直接测试文件的聚焦命令及 `npm run verify`。两者均在 Vitest 加载 `vite.config.ts`、启动 esbuild 子进程时遇到 `spawn EPERM`，测试尚未开始，不能记为通过。`verify` 的 typecheck、lint、format:check、docs:check 均通过（66 个元法术、97 个 Markdown 文件）；`git diff --check` 通过。
- 审阅：已自查全部本任务文档差异，补清草案合并与审批修订边界、终态快照仍有存活写进程时的占用规则，以及未知格式事务必须先校验再恢复。未创建其他 Agent，未执行独立审查；无运行代码改动，未新增只复述文档内容的测试。
- 剩余：内核实现、迁移/降级工具与演练、独立审查、`npm run verify:full` 和最终体验由秘书统一组织；本任务不替代正式版本 Definition of Done。

## 自适应版本编排：运行纵向切片

- 输入与范围：只实现 `adaptive-project-office-2026-09-21` 已采纳 ADR 的纵向切片；不改游戏运行时，不创建其他 Agent，不迁移 `.daoyan-agent/` 现场，不提交或发布。
- 状态与迁移：`SecretaryState.version: 1` 和 `FormalVersion.schemaVersion: 1` 保持不变，新增各自 `orchestration.schemaVersion: 1` 可选扩展。旧快照读入时幂等补齐候选/对账或高风险完整阶段策略，历史依据标为 `legacy-unknown`；不补造批准、测试和交付。未来或损坏扩展显式停止自动写入和派发。
- 收件去向：只有 `direction` 进入版本路由；没有活动版本时创建 `charter-draft` 草案，未冻结且同范围时追加范围修订，可能改变承诺时建立制作人决策门禁，冻结后写入下一版本候选。问题、无目标回复和继续分别答复、保留或恢复已批准工作；请求 ID、意图、去向、版本、范围修订和公开理由持久化。
- 阶段与质量：风险和阶段策略按修订追加；立项评审、主策审核、独立 QA、最终制作人体验和归档不可跳过；不可逆/范围/升级策划决策作为制作人门禁。Feature 自测保存类型检查和定向测试，独立 QA 保存测试身份、代码修订、验收/集成/回归/缺陷复验；阻塞与高风险缺陷只有成功独立复验才能关闭并进入候选。
- 恢复：守卫在派发前对账全部 active、tracking、retry-wait 及活动指针项。delivered/review-ready 清理 retryAt、恢复计数和陈旧进程引用；review-ready 另存 awaiting-review，不推进正式版本审批；终态快照仍有归属进程时保留占用，直到进程退出。
- 公开日志：新增版本化 `public-events.jsonl`，按字段白名单保存输入、计划、动作、文件、测试、错误、公开决策/阻塞、时间和 token。私有推理字段直接拒绝，凭据和本机路径脱敏，未知 token 为 null；事件 ID 去重、游标续读，按 run 去重累计。看板优先读取新日志，旧运行继续用保守投影。
- 测试：新增/扩展 `version-lifecycle`、`secretary-state` 和 `public-work-log` 测试，覆盖迁移、未来格式拒绝、意图去向、全队列选择、固定门禁、独立 QA/缺陷复验、日志脱敏/游标/token。`npm run typecheck`、lint、格式、文档检查和 `git diff --check` 已通过；把相关 TypeScript 编译到系统临时目录后的无头冒烟也实际通过了阶段推进、QA 身份隔离、冻结范围路由、公开日志去重、凭据/路径脱敏与私有字段拒绝。聚焦 Vitest 三次、最终 `npm run verify` 两次均在加载 `vite.config.ts` 时因受限环境禁止 esbuild 异步子进程而报 `spawn EPERM`，测试代码未开始执行；`verify` 在此前的 typecheck、lint、格式和文档步骤均通过，最终测试步骤受阻，不能把整条门禁记为通过。
- 剩余风险：生产备份、迁移/降级演练、完整门禁、独立审查和最终制作人体验仍由秘书统一执行；当前切片没有用运行实现反向修改已批准产品语义。

## 自适应版本编排：接管补强

- 接管判断：保留已有编排、对账、日志和测试改动；针对规格已经公开列出的迁移缺口继续完成运行纵向切片，不触碰 `.daoyan-agent/` 生产状态，不覆盖无关现场。
- 状态安全：秘书与正式版本 normalizer 现在校验新增扩展的嵌套记录；待恢复正式版本事务在写 current/历史及删除事务文件前复用同一校验。未来扩展或损坏嵌套事务会停止恢复，三个文件保持原字节。
- 收件重放：pending intake 在跨状态写入前持久记录预期去向和目标版本。重放优先查找原目标及同一 requestId 的范围修订；若活动版本已切换且原操作未落盘，转为公开决策阻塞，不把消息改投新版本。
- 质量门禁：开发阶段存在未完成 work item 时不得进入 QA；已完成 Feature 必须有同范围修订的类型检查和定向测试。缺陷只能使用当前范围修订的独立复验关闭，范围变化会使旧复验失效。
- 测试：新增正式版本异常事务原字节保留、秘书/版本嵌套损坏拒绝、Feature 未完成/未自测阻塞和旧范围复验失效用例。`npm run typecheck` 与 `npm run lint` 通过；将相关模块以 `tsc` 编译到 `node_modules/.cache/daoyan-adaptive-smoke/` 后，纯 Node 无头断言通过编排迁移、嵌套拒绝、Feature 门禁、复验失效、异常事务三文件不变，以及公开日志去重/脱敏/私有字段拒绝。Vitest 和本次直接 tsx 即时冒烟仍因环境禁止 esbuild 子进程报 `spawn EPERM`。
- 剩余：跨状态收件各提交点的故障注入、生产备份与保留增量的降级工具/演练、独立审查、完整门禁和最终制作人体验由秘书统一收束。本任务按要求不执行 `verify:full`、commit、push、tag 或发布。

## 独立审查修复：保守收件、缺失快照和范围评审

- 问题与范围：修复普通回复误立项、既有运行缺少可解析快照仍被重派，以及相反方向因词面相似而自动合并三项高风险问题。
- 非目标：不变更游戏语义，不迁移生产状态，不提交、推送或打标签；沿用 v1 字段与既有批准事实。
- 验收：本地/语义不可用时礼貌回复不建版本，模糊输入持久待确认；缺失或损坏快照阻止整个队列派发且保留运行归属，证据恢复后重新对账；已过草案阶段的非同一方向必须范围评审，不能靠相似度批准。补充单元与真实 HTTP 回归，执行相关验证并据实记录结果。
- 实现：回复规则支持标点和补充致谢，明确新方向优先于致谢；没有语义结果、明确方向规则和匹配事实的消息保存 `needs-confirmation`，不创建执行项。移除已过草案阶段的相似度合并条件，非同一原文方向进入范围评审。既有运行缺失/损坏/未知状态快照时，保存 `missing` 对账记录、保留目录/轮次/进程引用并保持 tracking；协调器消费失败结果，禁止整队列派发。删除“干净工作区可无快照重开”的旧恢复逻辑，Feature 启动前再检查恢复文件；可恢复快照仍有写进程时等待退出。
- 测试与门禁：标准聚焦 Vitest 与 `npm run verify:full` 均在加载 Vite 配置时因 esbuild `spawn EPERM` 受阻，用例尚未执行。完整门禁此前的 typecheck、lint、format:check、docs:check 全部通过（66 个元法术、97 个 Markdown 文件）；覆盖率、沙盒、E2E、构建未执行。使用上文预编译方法（增加 `--strict`，缓存目录 `daoyan-review-safety`）执行四个真实测试文件，共 68 条通过：生命周期 33、秘书状态 22、守卫工具 10、公开日志 3。
- HTTP 验证边界：已增加正式测试夹具，覆盖归档后回复/模糊消息不新建版本、批准后的相反方向生成 pending 范围与 open 门禁，以及缺失/损坏快照和存活子 Agent 的阻塞/终态恢复。真实守卫额外启动尝试因无法读取 Windows 进程身份受阻。隔离预编译副本使用模拟的当前进程身份、无模型、派发开关开启，通过实际 HTTP 请求验证礼貌回复、待确认、明确方向建草案、相反方向范围门禁、缺失证据阻断及恢复 delivered 后解除；这不验证真实进程归属，也不替代真实子进程测试或制作人体验。所有临时产物仅在忽略的缓存目录，生产状态未动。
- 文档与交付：同步工作流、ADR-0016、规格、版本策划与当前状态；维持 v1 字段和旧批准，无生产迁移，无 commit、push、tag。完整真实环境门禁仍须复验，不能将本次修复标为正式版本验收完成。

## 架构与迁移边界接管复核：种子、存在性与冲突

- 输入与范围：只承接已批准版本 `adaptive-project-office-2026-09-21` 的架构决策与迁移边界任务；不重新立项、不新增 Agent、不提交推送或发布。入场时 11 个已修改文件均与本版本有关，涵盖秘书意图分流、缺失恢复证据阻塞及单元/HTTP 回归；保留全部现场。本次仅修改文档，不代替独立审查或秘书版本收束。
- 核对依据：读取当前 AGENTS、状态、工作流、架构不变量、ADR-0012/0013/0014/0016、内核规格及相关状态/日志代码与测试。九项游戏架构不变量保持不变，秘书、正式版本、PM、Channel 的状态所有权保持不变；制作人已批准方向，不重新请求相同批准。
- 公开计划与结果：核对现场 → 对照读写/恢复入口与测试 → 补充迁移放行条件 → 同步长期和版本文档 → 执行聚焦测试及快速门禁。已完成本任务文档修改与自查，生产迁移和独立验收仍待秘书组织。
- 新明确的边界：运行 current 缺失会读取仓库种子，隔离演练须同时隔离种子与关联运行目录；清单需记录文件不存在，回退不能遗留升级新增状态。当前 normalizer 以真假值判断扩展存在，不能证明拒绝 null/false/0 等损坏值；迁移工具须先显式校验。历史列表按更新时间投影及归档读时修复不能代替多快照核验或成为重新开发授权。公开日志只有进程内、同路径串行化，不能据此放宽单写者要求。
- 文件：补充 ADR-0016、内核规格、架构总览、当前状态与本版本 charter；规格将前轮预编译结果与本次验证分开，避免过时通过数被当作本轮证据。没有更改状态格式或运行代码，没有接触生产状态。
- 验证：执行 `npm test -- test/version-lifecycle.test.ts test/secretary-state.test.ts test/secretary-notice-guard.test.ts test/public-work-log.test.ts test/secretary-dashboard.test.ts`，Vite 加载配置时因 esbuild `spawn EPERM` 退出 1，用例未运行。随后执行 `npm run verify`：typecheck、lint、format:check、docs:check 全部通过（66 个元法术、97 个 Markdown 文件）；test 同样在启动前失败，整条 verify 未通过。本次没有运行预编译替代测试，也未运行 `npm run verify:full`。
- 剩余风险与交接：在支持测试子进程的环境重跑上述命令；补齐整组迁移校验、全提交点故障注入及保留增量的降级工具/演练。独立审查、完整门禁、真实体验由秘书统一执行；不能将本任务的设计采纳记作正式版本验收通过。

## 执行 Agent 接管交付：扩展存在性与迁移拒绝

- 时间：2026-09-21 04:15:43 +08:00。Token：当前执行环境不提供可审计用量，记为不可用。
- 输入与范围：只承接 `adaptive-project-office-2026-09-21` 自适应编排内核纵向切片；保留现场中秘书分流、对账、看板回归与 ADR/规格改动，不扫描无关游戏路线，不操作生产状态，不创建 Agent，不提交、推送、打标签或发布。
- 公开计划与进度：现场分类与直接文档/代码审查完成 → 聚焦测试尝试完成 → 迁移存在性缺口修复完成 → 回归与长期文档同步完成 → 快速门禁已执行。
- 动作与公开决策：保留 v1 信封和可选扩展；将秘书、事项和正式版本 normalizer 改为按自有属性存在性判定迁移。只有真正缺失扩展的旧状态才补齐；显式 `null`、`false`、`0` 和非法对象保留原值并停止写入/派发。秘书加载不再为真正缺失的旧字段人工制造 `undefined` 属性。未修改已批准产品语义。
- 文件：`scripts/version-lifecycle.ts`、`scripts/secretary-state.ts`、`scripts/secretary-notice-guard.ts`、`test/version-lifecycle.test.ts`、`test/secretary-state.test.ts`，以及 ADR-0016、内核规格、架构总览、当前状态和版本 charter。其余现场相关改动仅审查并保留。
- 测试与错误：标准五文件聚焦 Vitest 在加载 `vite.config.ts` 时因 esbuild `spawn EPERM` 未进入用例。用实际 TypeScript 测试及依赖预编译、禁用配置加载的单线程 Vitest 执行无头回归，4 文件 76 条全部通过（生命周期 37、秘书状态 26、守卫工具 10、公开日志 3）。HTTP 测试依赖守卫/子 Agent 进程，在同一环境因 `spawn EPERM` 和无法取得子进程身份未成功执行，不记为通过。
- 快速门禁：`npm run verify` 已执行；typecheck、lint、format:check、docs:check 全部通过（66 个元法术、97 个 Markdown 文件），最后的标准 Vitest 仍在配置加载时因 `spawn EPERM` 退出，因此整条 verify 不能记为通过。按要求未运行 `verify:full`。
- 剩余风险：需在允许子进程的环境重跑标准 Vitest/HTTP 集成与完整门禁；生产备份、整组快照/引用校验、全提交点故障注入、保留增量的降级工具/演练及最终制作人体验由秘书收束。

## 独立审查修复：完整门禁的唯一责任

- 问题：详细策划与 `agent-workflow` 已把最终代码树的一份可复用完整门禁证据归于 Feature PM，但 `workflow` 把同一 `npm run verify:full` 指给 Version QA 在版本收束执行，造成重复执行或无人负责的歧义。
- 决策：`npm run verify:full` 的唯一执行责任者和证据所属 scope 均为 Feature PM / Feature。它在最终代码树执行一次，证据绑定代码树、命令与配置指纹。Version QA 只消费仍匹配的事实，再生成独立的 Version scope 跨 Feature 集成、主线回归、迁移、打包与候选结论；候选代码使完整门禁失效时，回到 Feature PM 重建，不由 Version QA 重跑同一门禁。
- 文件与适用性：同步 `module-design.md`、长期工作流、测试说明、ADR-0016、当前状态和本日志。问题是策划合同冲突，未触及运行代码、`.daoyan-agent` 状态或后续 QA 产物，也不预造测试；因此没有为纯文档修复制造实现测试。
- 验证：`npm run docs:check` 通过（66 个元法术、98 个 Markdown 文件），`git diff --check` 通过；针对性检索确认现行工作流只规定 Feature PM 执行 `verify:full`，Version QA 只消费匹配事实。未运行 `npm run verify` 或 `npm run verify:full`：详细策划/纯文档修复使用 light Profile，完整门禁不属于本 Task。

## 自适应编排交付收口

- 问题：交付审查发现制作人等待路径缺少真实恢复点、复合“继续/恢复 + 承诺变化”仍可能被旧排期吞掉、旧状态 Feature/QA 修订冲突可误放行，以及看板从原始模型日志猜测公开内容四类风险；首轮修复后复核又确认等待快照状态仍会被守卫当作技术故障自动重试。
- 实现：Feature PM 在请求决定前保存 `waiting-producer` 快照，明确区别于技术故障 `recoverable`；制作人确认后可在干净且仅向前演进的工作区从同一空恢复点继续。自然语言将否定、删除、替换、兼容性和范围变化优先视为方向，即使同句包含“继续/恢复”。候选门禁在旧状态缺少显式基线时也拒绝 Feature 与 QA 修订冲突。项目中枢不再读取原始 `.log` 展示内容，原始日志仅在内部解析额度重试时间。
- 回归：新增真实 Feature PM 子进程测试，隔离 Git 仓库中实际触发发布决策并验证恢复点；扩展秘书意图、旧 QA 修订、原始日志隐私哨兵及等待快照跨重启 HTTP 测试。最终聚焦四文件 68 条通过。
- 完整门禁：最终 `npm run verify:full` 通过；18 个测试文件 214 条通过，覆盖率 statements 85.61%、branches 80.8%，资源沙盒、19 条 E2E 和生产构建均一次通过。前一轮 E2E 曾出现一次页面加载超时并在自动重试后通过，最终复验未复现。
- 独立审查：首轮报告 4 项 high，修复后复核确认其中 QA 修订与原始日志两项闭合，并补充等待状态与复合表达两项边界；后者继续修复后由独立终审确认闭合，结论为 pass，无 high/medium 遗留。生产备份、迁移/降级演练和候选版本制作人体验仍属于正式版本后续门禁，本轮不代替发布。

## 正式版本无阻塞续推与按需节点

- 问题：正式版本虽已有阶段策略和状态机，但 Feature 交付没有驱动内部阶段，秘书在没有待办、额度或外部阻塞时仍会停在详细策划；`skipped` 节点还被作为完成项计入固定 13 节点进度，造成状态与真实版本含义不一致。
- 语义：制作人明确要求，不适用节点显示“无需执行”并从进度分母移除；活动版本只允许停在制作人门禁、额度、账号或外部访问阻塞，普通内部节点必须持续派发、登记证据并推进。
- 实现：正式版本公共投影按当前范围的最新阶段策略标记无需执行节点，进度只计算适用节点。秘书事项编排增加可选的正式版本、阶段和范围修订关联；notice guard 为内部阶段建立去重持久任务，交付后登记节点文档、工作项、Feature 验证、独立 QA 或缺陷复验，再原子推进。没有缺陷时自动将缺陷修复标记为无需执行。方向、版本策划、立项评审、制作人体验和归档保持固定门禁。
- 恢复：阶段任务沿用 Feature PM 的恢复、进程身份、公开日志和额度阻塞合同；守卫每次事件协调都会重新读取正式版本，已交付但尚未写入阶段的运行会先补登记，失败则生成下一次内部修复任务，不要求制作人手动发送“继续”。
- 验证：新增生命周期进度、可选策划/测试节点和阶段任务去重测试；最终 `npm run verify:full` 通过，18 个测试文件 232 条测试通过，覆盖率 statements 85.61%、branches 80.8%，资源沙盒、19 条 E2E 和生产构建均通过。阶段桥接只采信 `report.json` 中“已交付”且独立审查 `pass` 的运行，不能把人工改写的终态快照冒充测试或交付证据。
- 首轮独立审查：发现 QA 阶段提交报告后候选修订漂移、存在工作项仍可 skip 开发、修复 Agent 冒充独立复验，以及 reduced 指令和任务拆分落库不完整。修复后 QA 绑定实际测试候选，开发 skip 拒绝任何非 skipped 工作项；缺陷修复与独立复验使用两个持久事项和不同身份；任务拆分必须提交经依赖校验的 `task-breakdown.json`，不再生成单一占位任务。纯文档版本会自动声明不适用的运行时节点。
- 第二轮独立审查：指出阶段 PM 的成功交付仍可能被错误提升为主策批准、全部工作项完成、QA 通过或全部缺陷修复。桥接层因此改为严格消费五类结构化阶段清单，逐项校验工作项/缺陷、命令、公开证据和测试候选；主策退回会自动重排，升级结论会建立制作人门禁，QA failed 会携带缺陷进入修复，复验失败会回到新修复轮次。测试事项修改 `docs/` 外文件、缺少当前阶段 Markdown 或遗漏任一 ID 时均拒绝推进。文档-only 自动裁剪改为显式保守语法，避免把“战斗日志展示”误判成纯文档。
- 第三轮独立审查：发现阶段提示中的风险措辞会让零 token 规划器把每个内部节点重新升级为制作人决策、主策要求修改后仍停在审核节点，以及候选阶段可能在 QA 后改变实现。正式阶段现以 `[formal-stage:*]` 进入确定性单任务计划，永不重新立项；审核退回复用生命周期的 `changes-requested` 回退；QA、缺陷复验和候选阶段同时检查公开报告与测试修订到当前提交的 Git 文件差异，`docs/` 外变化一律阻止推进。
- 第四轮独立审查：发现阶段推进刚进入制作人门禁时仍可能启动普通队列、并发版本写冲突会污染内存状态、合法 skipped 工作项被错误要求自测通过，以及已有工作项会绕过新 `task-breakdown.json`。协调器现于阶段驱动后重读正式版本并优先停在制作人门禁；阶段登记在克隆状态中计算且只在成功写入后消费事项，冲突则重读重试；跳过工作项保留公开理由但不登记虚假 Feature 验证；每轮任务拆分均校验并替换旧清单。
- 第五轮独立审查：补出默认 `direction` 起点错误依赖不存在的 `direction.md`、自动跳阶段路径的并发写冲突仍可能逸出，以及“无需任务拆分但执行开发”会形成空转。方向节点现以版本状态自身为证据直接进入策划；自动写冲突会重读最新版本，事件协调异常由守卫定时恢复；跳过任务拆分会原子联动跳过开发并拒绝隐藏工作项。最终聚焦 94 条回归及完整 232 条测试通过，独立复核结论为无 high/medium 发现。

## 自适应项目中枢：模块详细策划增量

- 问题与范围：为当前正式版本补齐 Agent 工作流效率的详细策划；只覆盖 Task、Feature、Version 的唯一责任、证据复用/失效、恢复、公开事件和耗时统计。保留现有工作台、状态所有权、v1 兼容与 `legacy-unknown` 的完整执行策略。
- 决策：执行 Agent 只做直接类型、定向测试或文档校验；Feature PM 汇总后执行一次快速门禁和独立审查，最终树保留一份完整门禁证据；Version QA 处理跨 Feature 回归、迁移、打包和候选验证。证据绑定作用域、输入输出、Git tree、路径、命令与配置指纹；只失效相交证据与下游。公开事件 ID 必含执行轮次和代码修订，耗时分为模型计算、命令执行、等待制作人、等待额度、恢复、空闲六类，未知旧值不补零。
- 非目标：不改 `.daoyan-agent/` 运行状态、不执行迁移/降级、实现、QA、候选、发布或制作人体验；不预造后续阶段 JSON 结论。
- 文件：更新本版本 `module-design.md`、长期工作流、常驻秘书职责、ADR-0016、当前状态和本日志。规划/纯文档阶段采用轻量 Profile；完整版本门禁由后续 Feature PM 在最终代码树执行并保留 Feature scope 证据，Version QA 仅复用匹配事实并完成独立 Version scope 收束。
- 验证：`git diff --check` 与 `npm run docs:check` 通过（66 个元法术、98 个 Markdown 文件）。按本任务要求运行 `npm run verify`：typecheck、lint、format:check 和 docs:check 通过；Vitest 加载 `vite.config.ts` 时因当前环境禁止 esbuild 子进程（`spawn EPERM`）退出，未开始任何测试，因此整条 verify 不能记为通过。未运行 `npm run verify:full`。

## 自适应项目中枢：主策审核模块详细策划

- 审核范围：只审查当前正式版本的 `module-design.md`，对照版本策划、ADR-0016、内核规格、架构不变量、长期工作流和阶段结果解析合同；不操作 `.daoyan-agent/`，不推进版本节点，不创建 Agent，不提交、推送、打标签或发布。
- 审核结论：通过。四模块范围、产品交互、状态所有权、Task/Feature/Version 唯一责任、证据复用与增量失效、恢复和六类耗时均与已批准方向一致；固定制作人门禁和游戏核心不变量未被改变，无需升级制作人。
- 审核修订：详细策划原有行为合同缺少逐项验证落点，本轮补入阶段策略、恢复、证据失效、增量复审、耗时与公开事件的测试矩阵，并限定任务拆分优先扩展直接相关测试，不预造后续阶段证据。
- 阶段产物：新增 `design-review.md` 公开结论和 `design-review.json` 结构化结论；同步 `status.md`。批准只表示可进入任务拆分，不代表实现、生产迁移、完整门禁、独立 QA、候选体验或版本验收完成。
- 验证：结构化结论通过 JSON 解析与字段核对；`npm run docs:check` 通过（66 个元法术、99 个 Markdown 文件），`git diff --check` 通过。首次 `npm run verify` 在本轮三个文件的格式检查处停止，定向格式化后重跑：typecheck、lint、format:check 和 docs:check 全部通过；Vitest 加载 `vite.config.ts` 时因当前环境禁止 esbuild 子进程而报 `spawn EPERM`，测试用例未开始执行，整条快速门禁不能记为通过。按任务要求未运行 `npm run verify:full`。

## 自适应项目中枢：任务拆分

- 问题与范围：把已审核的 Agent 工作流效率增量拆成可验证、可依赖的正式版本工作项；只处理 `adaptive-project-office-2026-09-21` 的 `task-breakdown` 阶段，不修改运行状态或实现后续阶段。
- 策略：范围修订 2 沿用 `legacy-unknown`，旧状态没有可缩减的阶段策略，因此完整执行。清单不把既有纵向切片、策划结论或 QA 结论当作占位任务。
- 结果：新增机器可读 `task-breakdown.json` 与公开 `task-breakdown.md`。七项稳定工作从证据合同开始，拆分 Task 轻量 Profile、依赖恢复和公开耗时，再汇总为 Feature 门禁/独立审查、Version QA 复用和隔离迁移降级演练；全部依赖均在同一清单内且无循环。
- 验收：每项摘要明确受影响合同与可观察结果；任务级直接检查、Feature 唯一完整门禁与 Version 独立验证的责任没有混淆。后续实现优先扩展直接相关的调度、生命周期、秘书状态/守卫、公开日志和看板测试，未预造开发、QA、缺陷或候选通过证据。
- 验证：JSON/依赖闭包校验通过（7 项、ID 唯一、无自依赖或循环、所有依赖均在清单内）；`npm run docs:check` 通过（66 个元法术、100 个 Markdown 文件），`git diff --check` 通过。任务拆分属于纯文档阶段，不应执行统一 `npm run verify`；此前错误启动的命令不作为本阶段证据。未运行 `npm run verify:full`，也未手工推进 notice guard 或正式版本节点。

## 任务拆分审查修复：轻量验证与逐项验收合同

- 问题与范围：独立审查指出任务拆分把 Feature PM 的统一快速门禁误写入纯文档阶段，并且清单没有逐项保留受影响路径和验收命令。本次只修复 `adaptive-project-office-2026-09-21` 的任务拆分合同、解析校验、直接回归与长期文档；不编辑 `.daoyan-agent`，不推进 notice guard/版本节点，不实现后续工作项，也不提交、推送或打标签。
- 决策：`task-breakdown.json` 的每项现在必须提供非空 `affectedPaths` 和 `acceptanceCommands`；guard 解析并保留它们，拒绝缺失字段的清单。Markdown 逐项展示同一范围与命令。任务拆分自身只运行 JSON、依赖、路径/命令和文档检查，明确不运行 `npm run verify`；该命令仍只属于后续 Feature PM 汇总快速门禁。
- 文件：`scripts/version-lifecycle.ts`、`scripts/secretary-notice-guard.ts`、其直接 guard 回归测试、本版本任务拆分 JSON/Markdown、`agent-workflow.md`、`status.md` 和本日志。保留所有既有 `module-design` 现场。
- 验证：`npm run typecheck`、`npm run docs:check`（66 个元法术、100 个 Markdown 文件）与 `git diff --check` 通过；独立 JSON 图校验确认 7 项 ID 唯一、依赖闭合/无自依赖/无循环，且每项都有非空路径和验收命令。`npm test -- test/secretary-notice-guard.test.ts` 在加载 Vite 配置、启动 esbuild 前因环境 `spawn EPERM` 退出，用例未运行；预编译后以禁用配置的 Vitest 重试仍在 Vite 路径解析阶段触发同一限制，因此不记为通过。没有运行 `npm run verify` 或 `npm run verify:full`。

## 任务拆分审查修复：公开耗时看板路径

- 问题与范围：独立审查发现 `public-timing` 把不存在的 `scripts/secretary-dashboard.ts` 作为公开耗时的实现与证据失效边界。本次仅更正当前正式版本任务清单及其公开投影；不创建虚假的看板实现文件，不改 `.daoyan-agent`、运行状态或后续实现。
- 决策：公开看板的 `/api/dashboard` 投影实际由 `scripts/secretary-notice-guard.ts` 提供，故该文件替换不存在路径；`test/secretary-dashboard.test.ts` 仍是对应 HTTP 看板验收，`scripts/public-work-log.ts` 与 `scripts/secretary-state.ts` 保留为事件和持久状态边界。
- 验证：逐项路径存在性检查确认清单全部 13 个唯一受影响路径均存在；JSON 与 Markdown 的 `public-timing` 范围一致，且不再引用 `scripts/secretary-dashboard.ts`。本阶段为文档/清单修复，未运行统一门禁或手工推进版本节点。

## 任务拆分审查修复：公开验收命令一致性

- 问题与范围：独立审查发现公开 Markdown 将 `feature-quality-loop`、`version-qa-reuse` 与 `migration-drill` 的直接验收命令概括为阶段说明或“若干组测试”，却同时声称与 JSON 逐项一致。本次只同步这三项公开命令；不改机器清单、运行状态、后续实现或版本节点。
- 修复：工作项表现在逐项逐字列出 JSON `acceptanceCommands` 中的命令，包含 Feature 的类型检查、三组定向测试、快速门禁与唯一完整门禁，以及 Version QA 和迁移演练各自的定向测试、E2E/构建命令。Markdown 与 JSON 均继续把 `verify:full` 限定为 `feature-quality-loop` 的唯一 Feature scope 命令。
- 验证：将 Markdown 表中七项的反引号命令逐项与 JSON `acceptanceCommands` 比对，集合与顺序一致；随后运行 JSON 解析、文档检查与差异空白检查。未运行 `npm run verify` 或 `npm run verify:full`，未手工推进 notice guard 或正式版本节点。

## 自适应项目中枢：版本排期

- 问题与范围：只承接 `adaptive-project-office-2026-09-21` 的版本排期；把已核验的七项工作按依赖、风险和容量冻结为可执行序列，不创建其他版本或 Agent，不编辑 `.daoyan-agent`，不手工推进 notice guard 或版本节点。
- 排期结论：范围修订 2 延续 `legacy-unknown`，保守完整执行。关键路径是证据合同 → Task 轻量 Profile → 选择性恢复 → Feature 质量收束 → Version QA 证据复用 → 隔离迁移/降级演练；仅 Task Profile 与公开耗时统计可在基础层并行，Feature 汇总之后严格串行。容量固定为 7 项、一个基础并行窗口和一个最终 Feature 完整门禁所有者。
- 范围冻结：只覆盖 `scripts/` 编排、秘书状态/守卫、公开工作日志/看板投影及直接测试；不改游戏核心、玩法、DSL、存档、战斗、钉钉协议或生产运行状态。新增承诺按冻结规则进入范围评审或下一版本候选，不插队本版本。
- 文件：新增本版本 `version-planning.md`；同步 `workflow.md` 的排期产物合同、`status.md` 和本日志。ADR-0016 已定义状态兼容、证据和迁移边界，本阶段未作新的架构决定，未改写 ADR 或 `module-design.md` 现场。
- 验证：JSON 图校验通过（7 项、ID 唯一、依赖闭合且无循环、每项均有路径和命令）；`npm run docs:check`（66 个元法术、101 个 Markdown 文件）及 `git diff --check` 通过。版本排期是纯文档轻量阶段，未运行 `npm run verify`、Feature 快速门禁或 `npm run verify:full`。

## 自适应项目中枢：版本排期范围修订 3

- 问题与范围：将已批准的工作流审计缺陷澄清纳入当前 `adaptive-project-office-2026-09-21` 的版本排期，不新建版本、Feature 或制作人门禁；只修改排期、任务清单和必要长期工作流/架构/状态文档，不触碰 `.daoyan-agent`、`module-design.md` 未提交现场或后续实现。
- 冻结结论：容量仍为 7 项、基础层最多 2 项并行、Feature 汇总后严格串行。关键路径不变：证据合同 → Task 轻量 Profile → 选择性恢复 → Feature 质量收束 → Version QA 复用 → 隔离迁移/降级演练；`version-qa-reuse.owner` 更正为 Version QA。
- 审计收束：恢复只覆盖进程/主机/守卫异常、外部服务或额度阻塞、持久证据损坏；普通测试、格式、审查 finding 与局部修复在同一 PM 按未关闭 finding 或失败命令循环。启动阶段采用 `bootstrapping` 宽限，PM/dispatcher/worker 分离对账并在退出后重读终态；完整门禁每个 Feature 最终树一次，pre-push 复用匹配 Git tree 与配置指纹的证据；公开状态区分真实启动、异常恢复和局部修复轮次，并在交付后清理陈旧摘要。
- 回归计划：实现阶段须覆盖启动快照竞态、PM/worker 退出竞态、第二轮新 finding 原进程闭环、格式失败不恢复及 pre-push 证据复用；本排期阶段只核对 JSON、文档和依赖容量，不生成开发、QA 或完整门禁证据。
- 验证：`git diff --check`、JSON 图校验（7 项、ID 唯一、依赖闭合、无循环、逐项非空路径/命令）和 `npm run docs:check`（66 个元法术、101 个 Markdown 文件）通过。版本排期采用轻量文档 Profile，未运行统一 `npm run verify`、Feature 快速门禁或 `npm run verify:full`；因此不存在本阶段的子进程环境验证阻塞。

## 版本排期交付门禁修复：守卫夹具退出

- 问题与范围：独立审查的等待快照跨重启 HTTP 回归在断言失败后直接递归删除 Windows 临时夹具，仍存活的 `tsx` 守卫可能占用 `fixture`，导致次要 `EBUSY` 清理错误掩盖原始状态断言。本次只修复该测试清理路径；不改 `.daoyan-agent`、不推进版本节点、不改变既有排期范围，也不提交、推送或打标签。
- 修复：`secretary-dashboard` 的统一 `afterEach` 现在复用已验证的守卫停止助手并等待实际退出；嵌套 worker 也等待退出后才删除临时目录。显式成功路径仍保持原有停止逻辑，因此失败和成功两种路径都不会留下占用 junction 的子进程。
- 验证：`npx prettier --check test/secretary-dashboard.test.ts` 与 `node node_modules/typescript/bin/tsc --noEmit` 通过。目标 Vitest 命令在加载 `vite.config.ts` 前仍因受限环境禁止 esbuild 子进程而报 `spawn EPERM`，用例未执行；因此不把 HTTP 回归或完整门禁记为通过。未运行 `npm run verify` 或 `npm run verify:full`，符合 Task 的直接验证范围。

## 版本排期交付门禁修复：等待快照决定分流

- 问题与范围：独立审查的跨重启 HTTP 回归显示，等待制作人时的“采用方案 A，兼容已有存档”可被语义分流误判为新方向，原事项停在 `waiting-producer`，而非确认原恢复快照后的 `retry-wait`。本次只修复此确认边界及直接回归说明；不改 `.daoyan-agent`、不推进版本节点、不改变七项排期，也不提交、推送或打标签。
- 修复：守卫在已有等待快照且本地规则判定为 `reply` 或 `continue` 时，只有消息明确包含承诺变化才接受语义模型的 `direction` 结论；普通决定仍调用既有快照确认并进入 `retry-wait`。显式新增、取消、替换、限制等方向仍按原范围路由，不会被旧等待吞掉。
- 验证：目标 `npx vitest run test/secretary-dashboard.test.ts --pool=forks --poolOptions.forks.singleFork=true` 在 Vite 配置加载前被当前受限环境的 `esbuild spawn EPERM` 阻断，测试未开始，不能记为通过。`node node_modules/typescript/bin/tsc --noEmit`、修改文件的 Prettier 检查、`npm run docs:check`（66 个元法术、101 个 Markdown 文件）和 `git diff --check` 通过；以无配置单线程 Vitest 执行预编译的 `secretary-notice-guard` 直接回归，24 条通过，覆盖普通决定保留 `reply` 与显式承诺变化保持 `direction`。HTTP/子进程路径仍待允许 spawn 的环境复验；未运行 `npm run verify` 或 `npm run verify:full`，符合本修复 Agent 的定向验证范围。

## 自适应项目中枢：版本排期范围修订 4

- 范围：只完成当前正式版本的版本排期修订；不创建版本或 Agent，不编辑 `.daoyan-agent`，不手工推进节点，不改 `module-design.md` 现场，不提交、推送、打标签或发布。
- 排期结论：按 `legacy-unknown` 保守完整执行，冻结为 8 个稳定工作项。唯一允许的并行窗口仍是 `task-profile` 与 `public-timing`；`selective-recovery` 后串行执行新增 `secretary-backlog-reconciliation`，然后才允许 Feature 汇总、Version QA 复用与隔离迁移/降级演练。
- 对账范围：新增项以兼容迁移和原子写入关闭手机 Sites/手机优先候选（`cancelled` 或 `superseded`，钉钉为移动入口）以及已交付桌面工作台候选（`delivered`，关联已有交付记录和提交）。稳定关联必须让取消、替代、合并和交付反向闭合原候选；统计/问答应用状态覆盖去重，已发出的错误排期生成可去重更正通知。实现与回归直接归属秘书状态、guard、dashboard 及其三组测试。
- 验证计划：本阶段只解析 `task-breakdown.json`、核对依赖闭包和容量并运行文档/格式检查；不产生开发、QA 或完整门禁证据。后续 `feature-quality-loop` 是唯一 `npm run verify:full` 所有者，Version QA 仅复用匹配证据。
- 验证结果：JSON 图校验确认 8 项 ID 唯一、依赖闭合且无环；`git diff --check` 与 `npm run docs:check` 通过（66 个元法术、101 个 Markdown 文件）。本阶段适用轻量文档 Profile，未运行 `npm run verify`、Feature 快速门禁或 `npm run verify:full`；先前误记的统一快速门禁执行不构成本阶段证据。

## 自适应项目中枢：开发执行

- 范围与现场：只实现 `adaptive-project-office-2026-09-21` 的八项开发工作，不创建 Agent，不直接编辑 `.daoyan-agent`，不手工推进正式版本节点，不提交、推送、打标签或发布；`module-design.md` 保持未改。
- 证据与恢复：正式版本 v1 编排扩展新增可选 `validationEvidence`，绑定作用域、责任者、代码树/修订、路径、命令/配置、输入输出和执行轮次；失效先计算直接相交，再按输入证据图传播。Feature 恢复点保存 Task 输出/命令/配置指纹，恢复时保留有效任务和依赖闭包。旧证据缺字段保守不可复用，损坏扩展拒绝写入。
- 质量闭环：执行 Agent 提示改为只运行 Task 直接检查；规划/纯文档使用 light Profile。Feature PM 先完成快速门禁，再按失败命令和未关闭 finding 在原进程迭代，不再受固定两轮控制；最终成功的 `verify:full` 写入带代码树、配置和命令指纹的证据，pre-push 只复用完全匹配记录。Version QA 可绑定同修订 Feature 证据，但禁止重复 `verify:full`。
- 进程与公开状态：首次快照前使用 15 秒 `bootstrapping` 宽限；PM/worker 退出通知等待短暂原子落盘窗口并重读终态。公开事件 ID 强制附加执行轮次和代码修订，耗时按模型、命令、等待制作人、等待额度、恢复、空闲六类聚合；看板另投影真实启动、异常恢复和局部修复轮次，旧未知值为 null，终态清理陈旧摘要。
- 候选对账：秘书 v1 扩展新增可选 `itemResolutions`；normalizer 以稳定关系把手机 Sites/手机优先中枢候选标记为钉钉替代，把桌面工作台候选标记为已交付并关联 `fddcdcb`。原候选的取消、替代、合并或交付由关系反向闭合；排期统计先过滤终态并去重，更正通知使用持久 correlation ID。迁移仍通过既有原子 `saveState` 写入，不触碰生产现场。
- 迁移演练：新增隔离原始字节备份与校验，记录文件存在性和 SHA-256，拒绝源/备份根重叠；降级投影把旧格式事实和不可表达的编排增量分开保存。临时夹具演练通过，未执行生产停写、跨根迁移或真实回退。
- 定向验证：`node node_modules/typescript/bin/tsc --noEmit` 通过；修改范围 ESLint 通过。标准 6 文件 Vitest 首次尝试在加载 Vite 配置时被 `esbuild spawn EPERM` 阻断，未进入用例。随后把 5 个无头直接测试及依赖预编译到 `node_modules/.cache/daoyan-adaptive-development/`，用禁用项目配置/esbuild 的单线程 Vitest Node API 执行，141 条通过（生命周期 54、秘书状态 31、守卫工具 26、路由 26、公开日志 4）。真实 HTTP/子进程竞态不在该替代运行内。
- 阶段报告：逐项证据写入 `docs/versions/adaptive-project-office-2026-09-21/development.json`，公开结论写入同目录 `development.md`。按制作人明确要求，本执行 Agent 不运行 `npm run verify:full`；完整门禁、真正独立审查、生产迁移和 Version QA 由秘书后续阶段执行。
- 快速门禁：最终按任务要求运行 `npm run verify`，typecheck、lint、format:check 和 docs:check 全部通过（66 个元法术、102 个 Markdown 文件）；Vitest 仍在加载 `vite.config.ts` 时因 `esbuild spawn EPERM` 未进入用例，命令退出 1，所以本轮不把快速门禁记为通过。标准六文件聚焦命令同样在测试启动前受阻。未运行 `npm run verify:full`。
- 执行 Agent 交叉复核：发现审查提示还沿用“父进程已跑完整门禁”的旧顺序、Version PM 与 pre-push hook 会重复完整门禁、陈旧候选文本匹配会误关未来同类方向，且活动项与候选项共享稳定关系时分项计数可能重复。修复为快速门禁后的审查提示、Version PM 和 pre-push 都只消费代码树/配置/命令完全匹配的 `scripts/pre-push-verify.mjs` 证据，把兼容迁移限定到手机 Sites/手机优先 MVP 与桌面工作台增强的既有描述，并以活动状态优先生成统一有效事实集合。复核后 141 条无头测试全部通过；这不冒充独立 Agent 审查。

## 自适应项目中枢：开发执行独立审查修复

- 审查范围：只修复当前正式版本开发阶段报告的五项阻断问题；不新建版本，不编辑 `.daoyan-agent`，不手工推进节点，不改 `module-design.md`，不提交、推送或打标签。
- 退出竞态：主 `launch()` 的 child close 回调不再删除启动标记后直接单次对账，而是与附着式 PM/worker 监听共用有界终态快照重读；延迟出现的 `delivered`、`review-ready`、`waiting-producer` 或 `recoverable` 快照先被消费，避免包装进程先退时误记 missing/retry-wait。
- 证据复用：`reusableValidationEvidence` 现在必须接收并匹配当前 Git tree 与代码修订；Version QA 声明复用时同时匹配 Git tree、代码修订、命令与配置指纹，并核验 Feature 命令明细中存在退出码 0 的 `npm run verify:full`。同修订定向测试不能冒充完整门禁，Version QA 自身仍不得重跑该命令。
- 公开状态：公开事件构造不再为缺参调用补 `r1/unknown`，所有新事件必须提供真实轮次和代码修订。Feature PM 模型调用、命令、恢复/重试和跨启动等待，以及秘书的制作人等待、额度等待、恢复和空闲区间均写入实际持续时间；同名事件在新轮次或新修订下不会被旧 ID 去重。
- 排期一致性：秘书事项正式支持 `merged` 终态，并沿 `secretary:<id>` 稳定关系反向闭合原候选。更正通知由独立持久函数处理，外发成功后立即原子保存 `correctionSentAt`；即使本轮 normalize 和后续对账没有其他变化，重启后也不会永久显示待发送。
- 定向验证：`node node_modules/typescript/bin/tsc --noEmit` 与修改范围 ESLint 通过。标准四文件 Vitest 在加载 `vite.config.ts` 时因 `esbuild spawn EPERM` 未进入用例；将相同四个真实测试文件预编译到忽略目录 `node_modules/.cache/daoyan-review-five-findings/`，以禁用项目配置/esbuild 的单线程 Vitest Node API 执行，4 文件 118 条全部通过（生命周期 54、秘书状态 32、守卫工具 28、公开日志 4）。
- 完整门禁：`npm run verify:full` 已尝试一次；typecheck、全仓 lint、format:check、docs:check 通过，标准 Vitest 仍在加载配置、启动 esbuild 前报 `spawn EPERM`，所以命令退出 1，后续 coverage、sandbox、E2E 和 build 未执行。没有生成或伪造成功的完整门禁证据；需由允许子进程的 Feature PM 环境在同一最终树重新执行。

## 开发快速门禁修复：隔离守卫的公开代码修订

- 问题与范围：Feature 快速门禁的五个跨重启恢复场景在无 `.git` 的临时 fixture 中启动 notice guard 时，第一条通知调用严格 `git rev-parse HEAD` 并抛出“无法读取当前代码修订”，导致 HTTP 就绪前退出。本次只修复公开事件的修订来源及直接回归/阶段证据；不编辑 `.daoyan-agent`，不推进版本节点，不改 `module-design.md`，不提交、推送或打标签。
- 修复与边界：新增 `publicCodeRevision`。仓库运行继续使用 Git HEAD；隔离测试或部署副本缺少仓库元数据时，对 `package.json` 和 `scripts/` 的相对路径与内容生成确定性 `source-<sha256>`，保证公开事件仍绑定真实源码且源码变化使标识失效。`finalizeDeliveredVersionStage` 等正式阶段登记继续使用严格 `currentGitRevision`，源码哈希不能替代 Git 交付证据。
- 回归：`test/secretary-notice-guard.test.ts` 新增无 Git fixture，验证修订格式、重复读取稳定及源码修改后哈希变化。预编译实际测试与依赖并用禁用项目配置/esbuild 的单线程 Vitest Node API 执行，守卫工具 29 条全部通过。
- 验证：`npm run typecheck`、修改范围 ESLint、Prettier、`npm run docs:check`（66 个元法术、102 个 Markdown 文件）和 `git diff --check` 通过。标准聚焦 Vitest 在加载 `vite.config.ts` 时受宿主 `esbuild spawn EPERM` 阻断；预编译看板测试仍因沙箱禁止创建守卫子进程而不能运行 HTTP 场景，故不把原五个 HTTP 用例或完整快速门禁记为通过。未运行 `npm run verify:full`，等待允许子进程的原 Feature PM 环境复验快速门禁。

## 开发快速门禁修复：dispatcher 恢复点用例超时

- 问题与范围：`test/agent-dispatcher-recovery.test.ts` 会复制真实 `scripts/` 与 `agents/`、初始化临时 Git 仓库并启动 `tsx` dispatcher，内部已经允许子进程运行 20 秒，但外层 Vitest 仍沿用默认 5 秒；Windows fixture、Git 和工作区证据计算的正常开销因此会被误报为恢复点失败。本次只修复该测试预算和开发阶段公开证据；不改运行时恢复语义、不编辑 `.daoyan-agent`、不推进版本节点、不改 `module-design.md`，也不 commit、push 或 tag。
- 修复：抽出 `dispatcherTimeoutMs = 20_000`，外层 `integrationTestTimeoutMs` 明确比子进程多 10 秒。子进程超时仍会令 `result.status` 不是预期的 2 并使断言失败，测试没有通过延长外层预算掩盖真实挂起。
- 验证：`node node_modules/typescript/bin/tsc --noEmit` 与目标 ESLint 通过；目标文件经 Prettier 修复后检查通过，`git diff --check` 通过。标准目标 Vitest 在加载 `vite.config.ts` 时被宿主 `esbuild spawn EPERM` 阻断；禁用 Vite 配置的预编译尝试也因当前沙箱禁止 Node 创建 `git` 子进程而无法进入目标 dispatcher 路径，因此不把目标用例记为本环境已通过。该真实进程回归必须由原 Feature PM 在允许子进程的同一运行内重跑快速门禁。未运行 `npm run verify`、`npm run verify:full`、commit、push 或 tag。

## 开发交付前审查：生产验证链路与增量复核

- 问题与范围：独立审查确认 Profile、分作用域证据、一次性候选迁移、删除文件指纹和局部复核仍有五处生产接线缺口。本轮只修复当前正式版本开发阶段，不编辑 `.daoyan-agent`，不手工推进节点，不覆盖 `module-design.md`，不提交、推送或打标签。
- Profile：新增计划级有效 Profile 与实际验证阶段选择器。`light` 只保留 Task 直接检查；`task/feature` 执行快速门禁、独立审查和唯一完整门禁；`version` 执行版本任务与报告复核，不重放 Feature 快速/完整门禁。没有完整门禁责任的 Profile 推送使用 `--no-verify`，避免 pre-push 反向降级策略。
- 正式证据：完整门禁产物新增命令指纹、执行轮次和日志引用。notice guard 接纳 `development.json` 时逐项登记 Task 证据并以它们为输入登记 Feature 证据；接纳 QA/缺陷复验时从正式版本状态选择仍可复用的 Feature `verify:full`，把 evidence ID、Git tree、代码修订、命令和配置指纹传给 `recordQaRun`，随后登记 Version 证据。修复代码变更先按路径使旧证据及下游失效。
- 候选迁移：移除每次 normalize 执行的主题正则；一次性迁移只识别生产状态中手机候选 `42c7e743-2bdc-4152-aaf4-c8cd979df279` 和桌面候选 `85d190db-9843-4f83-a35f-ea0608fa9d7c`。以后含相同主题的新候选不会被自动关闭，正常反向闭合继续只消费显式 `secretary:<id>` 关系。
- 指纹与复核：pre-push 的代码树算法对缺失受控路径写入 `[deleted]`，与 dispatcher 一致。快速门禁首轮仍运行 `npm run verify`，失败后从 npm 输出识别具体子命令并只定向重跑该命令；独立审查修复前保存全文件边界，后续输入只列未关闭 finding 及边界后的 before/after 文件增量。完整门禁只在代码树变化后重新生成，pre-push 复用匹配证据。
- 回归：标准四文件 Vitest 在加载 `vite.config.ts` 时因宿主禁止 esbuild 子进程报 `spawn EPERM`，未进入用例。将四个实际测试文件与依赖预编译到忽略目录 `node_modules/.cache/daoyan-audit-fixes/`，用禁用项目配置/esbuild 的单线程 Vitest Node API 执行，4 文件 146 条全部通过（生命周期 54、秘书状态 33、守卫工具 30、路由 29）。`node node_modules/typescript/bin/tsc --noEmit`、修改范围 ESLint、Prettier 和 `git diff --check` 通过。最终树已执行 `npm run verify:full`：typecheck、全仓 lint、format:check、docs:check 通过，随后标准 Vitest 仍在配置加载阶段被同一 `esbuild spawn EPERM` 阻断，coverage、sandbox、E2E 和 build 未开始，因此不登记成功完整门禁证据；须由允许 esbuild/worker 子进程的 Feature PM 对同一最终树重跑。

## 开发交付前最终审查：退出归属、逐条通知与实际输出指纹

- 范围：只修复当前正式版本开发阶段独立审查报告的 3 个行为问题和 1 处公开错误文本；不编辑 `.daoyan-agent`，不手工推进版本节点，不覆盖 `module-design` 现场，不 commit、push 或 tag。
- 进程归属：`reconcileAfterProcessExit` 区分 PM 与 worker 退出来源。worker 退出后等待并重读快照，再按快照中的 PM 身份决定是否属于进程异常；PM 仍存活时事项保持 `tracking`、清理孤儿恢复计时并把进程引用重新绑定到 PM，避免立即或延后进入 `retry-wait`。
- 排期更正：`persistScheduleCorrections` 每成功发布一条就设置 `correctionSentAt` 并调用原子状态保存，再开始下一条。回归模拟第二条发布失败，确认重启只剩第二条待发送，第一条不会重复通知。
- 选择性恢复：恢复点新增声明路径的 `inputFingerprint`；`outputFingerprint` 改为覆盖任务期间实际 `changedFiles`。旧恢复点继续把历史输出指纹作为输入兼容值；新恢复点会在路径外直接依赖变化时失效当前任务及下游，同时仍检测声明输入变化。
- 公开文本：`publicCodeRevision` 在 Git 与源码身份均不可用时保持“无法读取当前代码修订，不能登记公开事件”，新增精确错误断言防止编码损坏。
- 验证：`npm run typecheck`、相关文件 ESLint/Prettier 和 `git diff --check` 通过。标准两文件 Vitest 在加载 `vite.config.ts` 时被宿主 `esbuild spawn EPERM` 阻断、未执行用例；预编译相同两个真实测试文件及依赖后，以禁用项目配置/esbuild 的单线程 Vitest Node API 执行 63 条全部通过（守卫工具 33、Agent 路由 30）。没有运行或伪造新的 `npm run verify:full` 成功证据。

## 开发交付前证据复审：退出身份、实时门禁与 Task 命令

- 问题与范围：独立审查发现主 `launch()` 包装进程退出仍可无条件结束存活 PM、Version QA 以候选证据自身指纹做恒真复用、开发接纳把计划 `acceptanceCommands` 全部伪造成退出码 0。本轮只修复当前正式版本开发阶段的这三项高优先级问题，不编辑 `.daoyan-agent`，不推进版本节点，不覆盖 `module-design.md`，不 commit、push 或 tag。
- 退出控制流：PM 与 worker 的退出回调都传递实际 PID/identity；等待并重读快照后，将退出身份与当前 PM 身份及存活状态核对。tsx/dispatcher 包装进程先退出但快照 PM 仍存活时，`observedExitEndsPm` 返回 false，`reconcileItem` 保持 `tracking` 并绑定当前 PM。
- 证据复用：`latestReusableFeatureGate` 的生产默认值改为实时计算候选树、`npm run verify:full` 命令指纹与验证配置指纹；Feature 门禁产物接纳也核验当前命令/配置。QA 和缺陷复验继续通过该统一入口，旧树或旧配置证据不能复用。
- Task 证据：`development.json` 的完成项新增实际 `commands` 与退出码；接纳逻辑直接使用这些命令形成指纹和证据，不再读取计划 `acceptanceCommands`，并拒绝把 `verify`、`verify:full`、coverage、sandbox、E2E、build、dist 或 release 登记为 Task scope。
- 验证：`npm run typecheck` 通过。标准两文件 Vitest 在加载 `vite.config.ts` 时因 `esbuild spawn EPERM` 未进入用例；预编译五个开发阶段无头测试并用禁用配置/esbuild 的单线程 Vitest Node API 执行，生命周期 54、守卫 34、秘书状态 33、路由 30、公开日志 4，共 155 条通过。该运行不替代标准 Vitest、真实 HTTP/子进程竞态或完整门禁。

## 开发交付前审查：阶段产物、只读依赖与稳定关系归一化

- 问题与范围：独立审查发现 QA/复验阶段必需的 `docs/versions/**` 产物会改变整树指纹并阻断 Feature 门禁复用；Task 只保存建议路径与实际输出，遗漏路径外未修改的只读直接依赖；普通入口创建的源候选没有自引用 `matchedFact` 时，会与指向它的活动后继重复计数。本轮只修复当前正式版本开发阶段的三项问题，不编辑 `.daoyan-agent`，不推进版本节点，不覆盖 `module-design.md`，不 commit、push 或 tag。
- 验证树：dispatcher、notice guard 和 pre-push 使用同一范围规则，排除正式版本流程在开发门禁后新增的 QA、复验与候选报告；代码、测试、其他长期文档和验证配置仍在树中。回归按开发→QA 顺序先登记 Feature 证据，再写入 `qa.json`/`qa.md`，确认复用成立；随后改动脚本，确认同一证据立即失效。
- 选择性恢复：Task 完成时以建议路径和实际改动文件为种子，解析相对 `import`、`export ... from`、动态 `import()` 与 `require()`，把解析到的仓库内只读直接依赖和建议路径共同保存为 `inputPaths`。恢复按持久清单重算输入指纹；旧恢复点继续保守回退到建议路径。新增回归保持只读依赖不在 `changedFiles`，仅改变输入指纹，确认当前 Task 及下游失效。
- 排期对账：`projectFactsFromItems` 先建立事项 ID 图并沿 `secretary:<id>` 找到关系根；被引用的源候选即使没有自引用，也与后继使用同一去重键。现有活动优先级继续确保 active/tracking 覆盖 backlog。回归移除原先掩盖问题的源自引用，并核对公开 pending/active/backlog 计数。
- 验证：`npm run typecheck`、相关脚本和测试 ESLint 通过。标准三文件 Vitest 在加载 `vite.config.ts` 时被宿主 `esbuild spawn EPERM` 阻断、未进入用例；预编译同三份真实测试及依赖到 `node_modules/.cache/daoyan-three-finding-fixes/`，补入原样 `pre-push-verify.mjs` 后，以禁用项目配置/esbuild 的单线程 Vitest Node API 执行，3 文件 99 条全部通过（守卫 35、秘书状态 33、Agent 路由 31）。该替代运行不冒充标准 Vitest 或完整门禁。

## 开发交付前审查：阶段复用、快速门禁续跑与独立事项计数

- 问题与范围：独立审查发现异常恢复会清空已通过审查并重放快速门禁、快速门禁在中段失败修复后跳过后续检查，以及无显式关系的同文案事项仍按文本键误合并。本轮只修复当前正式版本开发阶段，不编辑 `.daoyan-agent`，不推进版本节点，不覆盖 `module-design.md`，不 commit、push 或 tag。
- 阶段复用：恢复点新增 `validationProgress`，保存快速门禁已完成/待执行命令、独立审查通过结果、尝试轮次、候选验证树与配置指纹。恢复时只复用匹配事实；若同一最终树已有成功完整门禁证据，直接跳过快速门禁和独立审查，Git 交付前仍再次核验证据。
- 快速门禁：首轮继续执行聚合 `npm run verify`。失败时从 `package.json` 的 verify 链解析失败位置，保存此前完成命令和从失败点开始的队列；修复后逐个执行失败项及原先未运行的后续项，直至队列清空。局部修复后刷新保留阶段的树/配置指纹，避免异常恢复重放已通过步骤。
- 排期事实：`projectFactsFromItems` 仍沿显式 `secretary:<id>` 图解析关系根；没有该边的事项改用自身 ID，不再回退到规范化文案。新增两个完全同文案但无关联事项的回归，公开 pending 计数为 2。
- 验证：`npm run typecheck`、相关脚本与测试 ESLint、Prettier 通过。标准两文件 Vitest 在加载 `vite.config.ts` 时受宿主 `esbuild spawn EPERM` 阻断、未进入用例；将同两份真实测试和依赖预编译到忽略目录 `node_modules/.cache/daoyan-latest-audit-fixes/`，补入原样 `pre-push-verify.mjs` 后，以禁用配置/esbuild 的单线程 Vitest Node API 执行，2 文件 67 条全部通过（Agent 路由 33、秘书状态 34）。未运行 `npm run verify`、`npm run verify:full`、commit、push 或 tag。

## 开发交付前审查：验证输入、Task 依赖与崩溃恢复计数

- 问题与范围：本轮只修复独立审查指出的四项交付问题：验证树排除范围过宽、Task 依赖证据受报告顺序影响、`active` 崩溃快照恢复漏计异常次数，以及公开修订失败文本乱码风险。不编辑 `.daoyan-agent`，不手工推进版本节点，不覆盖 `module-design.md`，不 commit、push 或 tag。
- 验证树：dispatcher、notice guard 与 pre-push 共享精确白名单反面规则。后续审计进一步确认 `development.json`/`development.md` 在完整门禁前生成并被接纳链消费，因此也必须参与指纹；当前只排除门禁后生成的 `qa`、`bugfix`、`bugfix-reverification`、`candidate`、`producer-acceptance`、`archived` Markdown/JSON 报告。章程、范围、版本/模块规划、设计审核、任务拆分及开发逐项清单继续参与指纹；回归确认改变这些输入会拒绝复用旧 Feature 证据，而新增 QA 报告不会。
- 依赖与计数：`parseDevelopmentResult` 在字段、逐项对应和 DAG 校验后按 `dependsOn` 拓扑排序，notice guard 因此总能先登记上游 Task 证据再建立下游 `inputEvidenceIds`。恢复计数集中到纯函数；`active` 与 `recoverable` 恢复均增加异常恢复次数，`waiting-producer` 只增加真实启动次数，旧未知计数保持 null。
- 乱码：当前实现和测试统一使用“无法读取当前代码修订，不能登记公开事件”，精确断言已在真实测试中通过，不把乱码保留为兼容文本。
- 验证：`npm run typecheck`、相关文件 Prettier 与 `git diff --check` 通过。标准两文件 Vitest 在加载 `vite.config.ts` 时受宿主 `esbuild spawn EPERM` 阻断、未进入用例；预编译相同两份真实测试与依赖并以禁用项目配置/esbuild 的单线程 Vitest Node API 执行，2 文件 70 条全部通过（Agent 路由 34、守卫工具 36）。该替代运行不冒充标准 Vitest 或完整门禁。

## 开发交付前审计修复：真实选择性恢复、开发清单指纹与关系链闭合

- 问题与范围：只修复当前正式版本开发阶段审计指出的四项阻断和两处乱码核对；不编辑 `.daoyan-agent`，不手工推进版本节点，不覆盖 `module-design` 现场，不 commit、push 或 tag。`version-qa-reuse` 的 Task Breakdown 责任者已核对为 Version QA。
- 选择性恢复：dispatcher 在整体工作区拒绝之前先复算每个已完成 Task 的输入、实际输出、命令和配置指纹；当前变化全部落在计划路径、持久输入或实际输出范围内时进入逐项复用，范围外变化仍保守拒绝。新增真实 `--resume` 进程回归，覆盖非 takeover、工作区指纹变化、两个有效 Task 均跳过的生产控制流。
- 验证树：`development.json` 和 `development.md` 是完整门禁前产生、随后由 notice guard 消费的 Task 事实，现已纳入 dispatcher/notice guard/pre-push 共用的候选树；只继续排除 QA、缺陷、候选等真正后置报告。回归在门禁证据建立后篡改开发清单中的命令退出码，确认候选树与复用证据立即失配。
- 排期关系：`reconcileSecretaryBacklog` 对终态 successor 迭代到无状态变化，常见创建顺序 A、B、C 下，终态 C 在一次 normalize 中先关闭 B、再关闭 A；序列化重启保持无额外迁移和零待排期计数。
- 清单和文本：`selective-recovery`、`public-timing`、`secretary-backlog-reconciliation` 因直接进程/看板测试未在当前宿主执行，`development.json` 的 `targetedTests` 改为 failed 并记录实际退出码，不再把无头替代运行写成完整通过。公开提示为“逐一列出”，日志术语为“守卫夹具”；扫描 `scripts/`、`docs/`、`test/` 未发现 Unicode 替换字符。
- 验证：`npm run typecheck`、修改范围 ESLint 通过。标准聚焦命令在 Vite 配置加载阶段受 `esbuild spawn EPERM` 阻断；预编译同源逻辑测试后，4 文件 110 条及 3 文件 125 条分别全部通过。dispatcher 恢复进程用例在预编译运行中因 Node 无法创建 `git` 子进程而未执行成功；两条包含 `test/secretary-dashboard.test.ts` 的直接命令也未收集用例，故三项结构化状态保持 failed。最终树的 `npm run verify:full` 已实际执行，typecheck、全仓 lint、format:check、docs:check 通过后在同一 Vitest 配置加载限制处退出 1，coverage、sandbox、E2E、build 未开始，未生成或复用完整门禁成功证据。

## 开发交付阻断修复：选择性恢复的接管前现场基线

- 问题与范围：独立审查确认 `changesBelongToRecoveryPlan` 仍使用相对 HEAD 的完整当前脏路径，导致显式接管前已有且未变化的 `module-design` 等无关改动在后续异常恢复时再次参与归属并阻断普通 `--resume`；恢复点中的计划内文件全部还原到 HEAD 时，当前脏路径为空也会被误判为不安全。本轮只修复该选择性恢复边界及直接测试/文档，不编辑 `.daoyan-agent`，不推进版本节点，不覆盖既有 `module-design` 现场，不 commit、push 或 tag。
- 实现：v1 恢复点新增可选 `workspaceChangeBaseline`，保存当时相对 HEAD 的脏路径及 SHA-256 内容哈希，删除项保存 `null`。续跑比较恢复点与当前两份脏路径内容基线，只把之后新增、修改、删除或还原到 HEAD 的路径交给计划范围判断；不变的接管前现场被排除。旧恢复点没有该字段时保留原有完整脏路径保守判断，损坏的路径、绝对路径、父目录跳转或非法哈希会拒绝读取。
- 回归：真实 dispatcher 测试新增“已有无关脏现场 + `takeover` 恢复点 + 两个已完成 Task + 计划内新变化”，并断言普通 `--resume` 保留 `2/2` Task 且不覆盖无关文件；另新增恢复点内计划输出被删除、当前 `git status` 为空时仍能按恢复点增量归属的进程场景。纯函数回归同时确认未变化的无关路径不出现在增量中，而恢复点后新增与还原路径均会出现。
- 验证：`npm run typecheck`、修改范围 ESLint、Prettier、`npm run docs:check` 与 `git diff --check` 通过。标准 `npm test -- test/agent-dispatcher-recovery.test.ts --pool=forks --poolOptions.forks.singleFork=true` 在加载 `vite.config.ts` 时因 `esbuild spawn EPERM` 退出 1，未进入用例；预编译同一进程测试后，三个用例均在 Node 创建首个 `git` 子进程时受 `spawn EPERM` 阻断，不能记为通过。预编译 `test/agent-routing.test.ts` 并禁用项目配置/esbuild 后，36 条纯逻辑测试全部通过，覆盖恢复点路径内容差分；该结果不替代真实 `--resume` 进程回归或完整门禁。未运行 `npm run verify`/`verify:full`，未生成可复用完整门禁证据。

## 最终门禁失败增量：live-worker 终态对账等待

- 问题与范围：独立完整门禁已通过其余测试，仅 `secretary-dashboard` 的 live-worker 恢复场景在写入 `delivered` 快照后仍于固定 300 ms 读取到上一轮 `missing`。本轮只修复该测试同步边界和阶段证据，不改运行状态、不重放已通过门禁、不覆盖 `module-design`，也不 commit、push 或 tag。
- 修复：`/api/intake` 响应文件表示消息处理完成，运行对账由收件循环末尾继续执行；live-worker 分支还需做进程身份探测。回归改用 `expect.poll` 最长等待 10 秒，直接观察 `orchestration.reconciliationOutcome === 'delivered'` 后再核对 tracking/delivered 与 `processOccupied`。这保留了“恢复终态必须取代 missing”的行为断言，同时移除对机器调度速度的 300 ms 假设。
- 验证：`node node_modules/typescript/bin/tsc --noEmit`、`npx eslint test/secretary-dashboard.test.ts --max-warnings 0`、`npx prettier --check test/secretary-dashboard.test.ts` 和 `git diff --check` 通过。聚焦 `npm run test -- test/secretary-dashboard.test.ts -t "blocks an overdue existing run" --pool=forks --poolOptions.forks.singleFork=true` 在加载 `vite.config.ts` 时因当前宿主禁止 esbuild 子进程而报 `spawn EPERM`，未收集用例；结构化清单继续将关联的 `selective-recovery.targetedTests` 标为 failed，等待原 Feature PM 在允许子进程的环境复核，不把静态检查冒充目标回归通过。

# 工作流控制面直接维护与重复恢复修复

- 停止正在自我迭代的常驻秘书和 Feature PM，由主 Agent 直接接管工作流修复。
- 定位到 dispatcher 与 pre-push 使用两套候选树指纹算法，导致完整门禁虽已通过，提交后证据仍被误判失效，恢复随后重复执行快速门禁和独立审查。
- 统一候选树指纹来源；pre-push 自行运行完整门禁后持久保存可复用证据，并拒绝验证期间发生的追踪树变化。
- 精确的 Git 交付提交恢复只允许补跑缺失的最终门禁，不再重放上游模型审查。
- 增加阶段切换和 30 分钟长阶段摘要；控制面维护请求不会被秘书派给自身或下游 PM。
- 直接验证结果：关键跨进程套件 6 个文件、153 条用例通过；最终 `npm run verify:full` 通过，包含 18 个 Vitest 文件、覆盖率门禁、资源沙盒、19 条 Playwright E2E 和生产构建。
- 现场迁移：控制面正式版本退役归档后，notice guard 会关闭其遗留 PM 事项并清除活动指针，避免重启后再次恢复已由主 Agent 收口的旧任务；该归档不是大版本发布或候选验收。
- Windows 推送钩子不再直接 `spawn npm.cmd`；它通过当前 Node 进程启动 npm CLI，并在启动失败时输出实际错误，避免门禁尚未运行却只留下模糊的推送失败。

## 钉钉守卫重启配置刷新

- 修复桌面宿主早于钉钉配置创建时，`secretary:start` 继承旧进程环境、导致守卫误报“当前守卫未配置”的问题。
- Windows 启动器现在仅在进程缺值时从当前用户环境刷新钉钉配置；凭据不落项目文件、不输出日志，并继续从浏览器及 Feature PM 子进程环境剔除。
- 增加注册表输出解析、缺值补全和非 Windows 跳过的单元测试。

## 正式版本方向入口试跑修复

- 真实试跑发现多段产品方向末尾提及“不要纳入工作流控制面”时，控制面分类器会把其他段落中的“缺陷、阻塞”错误拼接为控制面维护动作。分类器改为只在同一语句同时出现控制面对象和维护动作时拦截。
- “新的正式版本、下一个版本、版本方向”现在属于明确方向信号；本地规则已经确认的明确方向不能被语义模型降级为“继续现有排期”。
- 增加否定边界说明和明确版本方向的回归测试。命令行多行参数仍不作为聊天传输方式，真实入口继续使用钉钉或中枢 JSON 收件接口。

## 统一实体创建、控制与属性模型：版本策划

- 范围：仅为 `draft-2026-09-21-b5fbefbd-c4c9-460e-935d-` 形成版本策划草案与公开阶段结论；未编辑 `.daoyan-agent`、未手工推进版本节点、未触及工作流控制面，也未提交、推送或打标签。
- 结论：创建伤害弹体将合并为一个玩家入口；实体控制统一采用 `(目标, 效果, 时间)`。用属性元数据的 `write`（一次性写入）与 `maintain`（持续维持）区分无限时间：前者只收一次资源，后者按结算间隔持续消耗法力与 tick，直到资源、主动结束或控制有效性终止；不按 Actor/Projectile 类型分叉。
- 定价：策划案要求效果强度、所属关系门槛、目标抗性和时间同时进入法力与 tick；己方仍有正成本，非己方有固定门槛，抗性单调加价。位置与旋转/朝向纳入可声明、可拒绝的统一属性能力。
- 后续：等待制作人评审策划案；通过后才进入详细策划，并先以 ADR 固定属性描述符、维持叠加/撤销、兼容迁移及创建入口的不可逆合同。
- 验证：执行 `npx prettier --check docs/versions/draft-2026-09-21-b5fbefbd-c4c9-460e-935d-/charter-draft.md docs/status.md docs/dev/2026-09-21.md`；仅文档改动，不运行统一 `npm run verify` 或 `npm run verify:full`。

## 统一实体创建、控制与属性模型：模块详细策划

- 范围：仅完成正式版本 `draft-2026-09-21-b5fbefbd-c4c9-460e-935d-` 的 `module-design`；读取版本策划、已采纳的统一实体/能力 ADR、当前元法术、World、VM/Battle 与直接测试。未创建 Agent、未编辑 `.daoyan-agent`、未手工推进节点、未提交、推送、打标签或发布。
- 结论：创建入口定为返回句柄且立即激活的 `创建弹道(方向, 速度, 伤害, 时间)`；控制以属性描述符和能力而非实体种类分派，每项具名节点归一为 `(目标, 效果, 时间)`。`时间=0` 表示无限，`write` 是一次性覆写，`maintain` 是施法实例拥有、每 0.25 秒持续结算的租约；`结束施法`、打断、余额不足或控制失效都会原子停止维持。
- 定价与边界：关系层延续自身/自有 `1`、友方 `2`、非己方 `8`，抗性只能单调抬高法力与 tick；己方保留正成本，非己方有固定门槛。位置、旋转、速度、伤害与寿命列为首批描述符，缺失能力确定性返回 `false`。敏感 Actor 资源属性不在本版本开放控制。
- 前置与后续：`时间=0`、覆写/租约生命周期、抗性与价格配置、旧调用诊断和回退是不可逆合同，开发前必须写 ADR。任务拆分、排期、开发、QA、候选体验和发布均不属于本阶段，未伪造其产物或证据。
- 验证：`npx prettier --check` 覆盖本阶段三份文档、`node scripts/check-docs.mjs`（104 个 Markdown）与 `git diff --check` 均通过。`npm run docs:check` 的元法术生成子步骤在当前宿主因 `esbuild spawn EPERM` 未执行；这不影响已通过的链接/Markdown 检查，亦不作为代码或完整门禁证据。

## 统一实体创建、控制与属性模型：主策审核

- 范围：仅审核并修订正式版本 `draft-2026-09-21-b5fbefbd-c4c9-460e-935d-` 的模块详细策划，产出 `design-review.md/json`；未编辑 `.daoyan-agent`、未手工推进版本节点、未触及工作流控制面，也未创建 Agent、提交、推送、打标签或发布。
- 审核发现：原四参创建入口遗漏旧 `发射` 的起点，不能无损保留任意起射位置；原属性表又把 Projectile 的伤害/速度与 Actor 的 power/speed 分成不同公开能力，未满足同一控制法术跨火球、自身和敌人复用。有限位置覆写还会产生到期回弹，周期 tick 也未区分执行工作量与可耗尽资源。
- 修订：统一创建改为 `(起点, 方向, 速度, 基础伤害, 存活时间)` 并固定旧调用的空间、伤害和资源等价锚点；实体控制改用“稳定逻辑属性 descriptor + 目标 binding”，同一 `damage`/`speed` 入口按 binding 明示 write 或 maintain。write 细分 commit/overlay，位置与寿命只接受 `时间=0` 的一次 commit；maintain 绑定核心控制会话，每 `0.25` 秒实扣法力并把 tick 计入施法执行债务。
- 结论：`approved`。修订后版本方向、架构不变量、迁移/回退、失败语义和双资源定价均完整，无需升级制作人；notice guard 登记后可进入任务拆分。实现前必须先以 ADR 固定逻辑属性/binding、控制会话、周期预算和兼容迁移合同。
- 验证：`design-review.json` 通过 JSON 解析、字段白名单和 decision 枚举校验；本阶段五份文档通过 Prettier 检查，`node scripts/check-docs.mjs` 通过（105 个 Markdown，版本 0.2.0），`git diff --check` 通过。按执行 Agent 约束未运行 `npm run verify` 或 `npm run verify:full`。

## 统一实体创建、控制与属性模型：任务拆分

- 范围：仅为正式版本 `draft-2026-09-21-b5fbefbd-c4c9-460e-935d-` 交付可审计工作项清单；读取版本策划、详细策划、审核结论、工作流、直接核心/战斗/界面路径和相关测试。未创建 Agent、未编辑 `.daoyan-agent`、未手工推进版本节点、未触及工作流控制面，也未提交、推送、打标签或发布。
- 拆分结论：建立 11 项稳定 ID 工作项，依赖顺序为 ADR 不可逆合同 → 无头属性/binding 与控制会话/预算 → DSL 兼容迁移与统一元法术 → 战斗、持久化、UI/参考 → 跨层回归。每项在 JSON 中都有同清单依赖、非空受影响路径和直接验收命令；未用笼统占位任务替代实际实现、迁移或验证范围。
- 关键边界：`adr-control-model` 是所有实现的硬前置，采纳前不得并行实现元法术或 UI；`entity-model-integration` 覆盖创建、迁移诊断、跨实体属性、双资源单调性、无限维持停止和能力拒绝，但其计划命令不作为当前阶段的成功证据。普通缺陷将进入版本测试统一闭环。
- 文档同步：新增 `task-breakdown.json` 与 `task-breakdown.md`，更新当前状态以反映“等待登记后版本排期”，未把计划或后续门禁写成已完成。
- 验证：将执行 JSON 结构/依赖/范围/命令检查、Prettier、文档检查和差异检查；按规划/纯文档 Profile 不运行 `npm run verify`、`npm run verify:full`、E2E 或构建。

## 统一实体创建、控制与属性模型：版本排期

- 范围：仅完成正式版本 `draft-2026-09-21-b5fbefbd-c4c9-460e-935d-` 的 `version-planning`；依据已核验的任务拆分、详细策划和主策审核冻结依赖顺序、容量、并行边界与范围。未创建 Agent、未编辑 `.daoyan-agent`、未手工推进版本节点、未触及工作流控制面，也未 commit、push、tag 或发布。
- 排期结论：11 项工作按 ADR → 属性 binding → 控制记录 → 周期预算 → DSL 兼容 → 统一元法术推进；随后仅允许战斗桥接与法术书/持久化两项并行，最后严格依序完成 UI 反馈、参考更新和跨层集成。关键路径经过战斗桥接；没有隐藏 Feature 或外部依赖。
- 范围冻结：只覆盖统一弹体创建、三参数属性控制、`write`/`maintain`、关系/抗性双资源定价、兼容迁移与玩家反馈；排除 ECS、实体原型、随机控制、冷却、敏感资源属性、工作流控制面和生产运行状态。后续改变承诺的新增方向只能进入范围评审或下一版本候选。
- 验证：将以 JSON 解析与 DAG/容量断言、Prettier、`node scripts/check-docs.mjs` 和 `git diff --check` 复核；按规划/纯文档 Profile 不运行 `npm run verify`、`npm run verify:full`、E2E 或构建，不把计划验收命令写作已通过事实。
