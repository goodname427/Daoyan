# 2026-09-24 开发日志

## 正式版本独立 QA 重验

- 范围：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-` 的版本测试；只运行验收、集成、回归相关检查并更新阶段报告。非目标：修改产品实现、手工推进正式节点或发布。
- 基线：`HEAD=c725e334fdebc09dfd7012ad971250841dfb4b95`；旧 QA 的通过结果早于法力账本修复 `f065d2c`，不能沿用。开工时工作区干净。
- `npm test -- test/entity-vnext.test.ts test/core.test.ts test/combat.test.ts test/dsl-migration.test.ts test/persistence.test.ts test/render.test.tsx test/review-regressions.test.ts`：退出 1；加载 Vite 配置时 `esbuild spawn EPERM`，零断言。
- `npm run typecheck`：退出 0。
- `npm run test:e2e`：退出 1；Playwright 在创建 worker 时 `spawn EPERM`，零浏览器用例结果。
- `npm run build`：退出 1；加载 Vite 配置时 `esbuild spawn EPERM`，本轮无新构建产物。
- 结论：[独立 QA](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/qa.md) 标记 `blocked`，未确认新产品缺陷，缺陷数组为空。需要可启动子进程的宿主重跑失败命令并实际验收当前修订；不把旧通过结果或类型检查当成完整版本 QA。

## QA 宿主补验与控制面对账

- `qa-8` 隔离宿主的 `spawn EPERM` 原始结果保留；`qa-9` 在环境重试中尚未完成任何工作项便被暂停，以阻止重复消耗。主 Agent 在可运行宿主重跑原定向命令，7 文件 220 条通过，其中减速预设按 `0.016` 秒更新的法力账户守恒回归通过；`npm run typecheck`、20 条 Chromium E2E 与 `npm run build` 均退出 0，构建 237 个模块。
- 旧 QA 候选修订与当前修订之间仅有控制面及文档变化；正式节点的产品实现差异校验原本漏掉 `recover-candidate`、`rollback-stale-qa`、`version-lifecycle` 及其测试，导致明明是控制面恢复也反复退回 QA。补充明确白名单及回归，不扩大到游戏脚本或游戏测试。正式版本节点和完整门禁仍待 notice guard 基于实际提交树核对，不手工改运行状态。

## 候选法力微单位缺陷独立复验

- 问题与范围：仅复验 `candidate-micro-mana` 的余额不足反噬账目，不改产品代码、游戏测试或运行状态。验收为原 `0.016` 秒减速战斗路径确实反噬、逐帧微单位等式与世界能量账本守恒。实际修复提交为 `f065d2c`；策略所列 `707f812` 是工作流恢复提交。当前 `HEAD=46ab5ab1bfaf8b4dbe726060db774f4d59d07fc4`，相关产品及复验测试相对修复提交无差异。
- `npm test -- test/combat.test.ts test/entity-vnext.test.ts test/review-regressions.test.ts` 退出 1：加载 Vite 配置时 `esbuild spawn EPERM`，零条测试执行。临时在忽略的 `node_modules/.cache/` 放置进程内 TypeScript 转译 loader 和独立无头脚本；执行 `node --no-warnings --experimental-loader ./node_modules/.cache/daoyan-micro-mana-loader.mjs ./node_modules/.cache/daoyan-micro-mana-reverify.mjs` 退出 0。
- 无头脚本导入减速预设、绑定鼠标、设置法力 400/神识 80、把目标放在 120 距离，逐帧断言 150 次账户精确等式、`manaAccountSnapshot.conserved` 和世界能量账本守恒；第 40 帧出现反噬，共 1 次。第 9/40/150 帧余额分别为 `131272000`、`46775999`、`89015999` 微单位，均守恒。`npm run typecheck` 退出 0。
- [公开缺陷报告](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/bugfix.md)及[结构化复验结果](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/bugfix-reverification.json)标记该缺陷复验通过；标准 Vitest 与本轮浏览器候选交互没有成功运行，旧 QA 和完整门禁只作既有背景。notice guard 负责正式登记及后续节点，不手工推进；本轮未 commit、push、tag 或发布。
