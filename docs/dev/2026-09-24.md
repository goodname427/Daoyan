# 2026-09-24 开发日志

## 版本节点任务与 PM 分层

- 制作人确认先对齐产品大方向，再让一个逻辑 Version PM 在每个节点拆成果，由一项成果对应一个 Feature PM。控制面改动在隔离工作树 `codex/stage-task-pms` 实施，不创建新的游戏正式版本，也不迁移旧归档状态。
- 新版节点取消独立任务拆分和版本排期；节点任务合同含来源范围修订、依赖、读取/写入路径、验收与交付物。秘书只按依赖串行派发和接纳结构化证据，Feature PM 自行规划执行、独立审查并单项提交，Version PM 收束阶段。独立 QA、复验、候选只允许写版本证据目录；越界交付阻断并由主 Agent 核查。
- 开发集成树由阶段收束运行一次完整门禁；QA 只复用匹配证据并补跨项验证，修复改变树后重新验证。新版本在独立集成分支本地提交，候选通过制作人门禁并归档后才快进合回并推送主干；推送失败可从已合并的本地主干继续。
- `npm run verify` 首轮共 477 项通过、12 项看板进程测试失败；原因是新工作树没有 `.daoyan-agent` 根目录，notice guard 在初始化前启动递归监视。补齐目录初始化后，定向看板 20 项、节点任务 7 项、Git 交付 1 项通过。
- `npm run verify:full` 随后退出 0：492 条基础测试、覆盖率、资源沙盒、24 条 Chromium 流程和生产构建均通过。再添加新版节点任务看板浏览器用例，用合成正式版本实看两项交付及 Feature PM、提交和证据详情；旧拆分和排期节点不显示，定向 Chromium 用例通过，截图见本地 `test-results/stage-tasks.png`。该合成用例不代表下一正式版本的真实 PM 成本和返工数据。
- 差异审查补强了阶段规划与收束必须独立提交、规划仅能提交本轮任务清单及说明、越界提交停止自动重派，以及归档后仍可调度其他秘书事项。最终树完整门禁退出 0：492 条基础测试、覆盖率、资源沙盒、25 条 Chromium 流程和生产构建通过。Git 交付以最终提交与推送记录为准。

## 浏览器测试误发钉钉消息

- 制作人收到周期重复的“可视化秘书验收版本”进度和归档消息。该标题并非当前正式版本，而是 `e2e/secretary-dashboard.spec.ts` 中的测试夹具。此前多次完整门禁启动临时 notice guard，子进程从父环境继承真实钉钉凭据；`DAOYAN_SECRETARY_LOCAL_ONLY=1` 只停用了语义分流，没有停用外部通讯。浏览器测试发送进度询问、点击推荐待办，真实钉钉通道因此向制作人发送了测试答复。重复周期与完整门禁的多次执行对应，不是另一个正式版本反复归档。
- `secretaryChannelHubFromEnvironment` 在 local-only 模式直接返回零外部通道，阻断钉钉和 webhook 连接；单元回归在注入两种外部通道配置时断言零通道、零投递，浏览器回归在测试对话前核对 `channels.json`。真实常驻秘书的非 local-only 配置不变。排查期间生产 notice guard 保持停止，待完整门禁验证后再恢复。
- 旧的通知 outbox 重试测试此前同时设置 local-only 和本机模拟 webhook；新边界正确阻断后，将该单独用例改为通讯允许、只配置 `127.0.0.1` 模拟 webhook，并显式清空钉钉凭据。定向单元测试和两条项目中枢浏览器流程通过；完整门禁和推送结果另行记录，不把定向结果冒充完整通过。
- 最终 `npm run verify:full` 退出 0：484 条基础测试、覆盖率、资源沙盒、24 条 Chromium 流程与生产构建通过。恢复正式 notice guard 后，当前正式版本仍为 `archived`，活动任务为空，钉钉 Stream 在线，待发通知为 0；测试夹具与正式通道已隔离。本版专用自动跟进仍保持删除，不自动建立下一版。

## 制作人代理验收与正式归档

- 制作人明确要求将当前版本推进到完成，以便准备下一版。开工核对主仓库和两个附属 Git 工作区均无未提交受控文件；`.daoyan-agent/`、`dist/`、`coverage/`、`test-results/` 是正常忽略的本地产物，不当作待提交源码。
- 当前候选三个产物 SHA-256 与[结构化证据](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/candidate.json)一致，游戏实现和构建配置相对候选修订无变化。重新运行生产预览的 3 条 Chromium 体验，全部通过；核对正式 QA、`candidate-micro-mana` 复验和零开放缺陷。
- 通过 `applyVersionTodoDecision` 和 `writeFormalVersion` 正式处理候选体验待办。首次原子替换 `current.json` 遇到 Windows `EPERM`；保留的写事务经 `readFormalVersion` 自带恢复成功，随后核对当前与历史快照均为修订 19 的 `archived`，待办与开放缺陷均为 0，事务文件已清除。没有手工编辑运行状态。
- 代理验收和归档分别留档于版本目录；这是内部版本闭环，不是大版本发布。新的产品方向仍须由制作人提出并单独立项。
- 归档文档交付门禁首次 `npm run verify:full` 在 `test/secretary-dashboard.test.ts` 的夹具清理遇到瞬时 Windows `EBUSY`：478/479 条基础测试通过，文件占用发生在 `rmdir`，不是候选产品断言失败。该用例单独复跑通过；第二次完整门禁退出 0：479/479 基础测试、覆盖率（91.15% 语句）、沙盒、23/23 Chromium E2E 和生产构建全部通过。保留第一次失败事实，不据此改写游戏代码或候选证据。

## 候选阶段控制面止损与本机补验

- 制作人要求暂停后，停止 notice guard 与候选 PM，并暂停本线程自动跟进。候选 Agent 的 `workspace-write` 隔离宿主连续遭遇 `esbuild spawn EPERM`、浏览器请求头策略错误；旧候选运行出现六轮同类审查修复，新一轮又只产出环境阻断文档。问题不在游戏实现，而在环境阻断未于审查前停止，且正式候选节点只凭报告与 Markdown 就可能推进。
- 候选任务现在须提供 `candidate.json`；阻断结果在进入独立审查前终止并使当前正式节点停住，不再重派。通过结果须携带真实构建命令与退出码、源码修订、产物哈希、从构建产物运行的三条浏览器体验及公开证据；正式对账再次核对阶段时间、产品代码差异、磁盘文件哈希和证据文件。新增本机补验入口与受审计接纳入口，后者在证据提交且工作区干净后才能推进正式节点。
- 本机对当前游戏源码重新构建成功；初版候选浏览器脚本因未展开预设列表及未收起遮挡面板失败，修正脚本后从生产预览跑通 `candidate-journey`、`candidate-narrow`、`candidate-ledger` 三条 Chromium 流程，未修改游戏产品代码。结构化结果与 SHA-256 见版本目录的 `candidate.json` 和 `candidate-browser.json`。这不是隔离 Agent 成功，也不是制作人验收或归档。
- 最终树 `verify:full` 通过：479 条单元/集成测试、覆盖率、沙盒、23 条 Chromium E2E 与生产构建；提交 `d0b0862` 已推送。候选接纳入口先只读预检，再在 notice guard 停止且工作区干净时核对已提交证据、QA 基线和构建文件哈希，通过正式生命周期 API 将当前版本推进到 `producer-acceptance`。候选节点完成不等于制作人验收通过；本轮未归档、tag 或发布。

## 当前候选构建核对

- 问题与范围：为同一正式版本形成可体验候选，核对构建、已测源码和实际页面入口；非目标是改产品功能、重跑 Feature 完整门禁、推进运行状态或发布。开工 `HEAD=b791f53d634a5ac136dd166c98fa4bddbfbf925e` 且工作区干净。
- `git diff --quiet 0515b4b7c0357cc37300c6dff1d027c1410d5024 -- src e2e test/combat.test.ts test/entity-vnext.test.ts test/review-regressions.test.ts package.json package-lock.json vite.config.ts electron` 退出 0；已有 QA 和缺陷复验仍是同产品源码背景。`npm run typecheck` 退出 0。
- `npm run build` 退出 1：Vite 配置加载遭 `esbuild spawn EPERM`，未生成新构建。现有 `dist/` 的 SHA-256 已列在[候选报告](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/candidate.md)，不能把旧文件登记为本轮新构建。
- 用 Python 提供 `dist/` 静态文件，本机 HTTP 返回 200；Edge 浏览器控制在枚举或打开候选页时反复返回 `Unable to load browser request-header policy`，未完成推演、演武、窄屏或控制台检查。当前阶段结论为阻断；恢复需可构建、可浏览宿主的同源码新构建和真实页面操作。未运行 `verify` 或 `verify:full`，未改 `.daoyan-agent`，未 commit、push、tag 或发布。

## 正式版本独立 QA 重验

- 范围：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-` 的版本测试；只运行验收、集成、回归相关检查并更新阶段报告。非目标：修改产品实现、手工推进正式节点或发布。
- 基线：`HEAD=c725e334fdebc09dfd7012ad971250841dfb4b95`；旧 QA 的通过结果早于法力账本修复 `f065d2c`，不能沿用。开工时工作区干净。
- `npm test -- test/entity-vnext.test.ts test/core.test.ts test/combat.test.ts test/dsl-migration.test.ts test/persistence.test.ts test/render.test.tsx test/review-regressions.test.ts`：退出 1；加载 Vite 配置时 `esbuild spawn EPERM`，零断言。
- `npm run typecheck`：退出 0。
- `npm run test:e2e`：退出 1；Playwright 在创建 worker 时 `spawn EPERM`，零浏览器用例结果。
- `npm run build`：退出 1；加载 Vite 配置时 `esbuild spawn EPERM`，本轮无新构建产物。
- 结论：[独立 QA](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/qa.md) 标记 `blocked`，未确认新产品缺陷，缺陷数组为空。需要可启动子进程的宿主重跑失败命令并实际验收当前修订；不把旧通过结果或类型检查当成完整版本 QA。

## QA 宿主补验与控制面对账

- `qa-8` 隔离宿主的 `spawn EPERM` 原始结果保留；`qa-9` 在环境重试中尚未完成任何工作项便被暂停，以阻止重复消耗。主 Agent 在可运行宿主重跑原定向命令，7 文件 220 条通过，其中减速预设按 `0.016` 秒更新的法力账户守恒回归通过；`npm run typecheck`、20 条 Chromium E2E 与 `npm run build` 均退出 0，构建 237 个模块。
- 旧 QA 候选修订与当前修订之间仅有控制面及文档变化；正式节点的产品实现差异校验原本漏掉 `recover-candidate`、`rollback-stale-qa`、`version-lifecycle` 及其测试，导致明明是控制面恢复也反复退回 QA。补充明确白名单及回归，不扩大到游戏脚本或游戏测试。正式版本节点和完整门禁仍待 notice guard 基于实际提交树核对，不手工改运行状态。

## 候选法力微单位缺陷独立复验

- 问题与范围：仅复验 `candidate-micro-mana` 的余额不足反噬账目，不改产品代码、游戏测试或运行状态。验收为原 `0.016` 秒减速战斗路径确实反噬、逐帧微单位等式与世界能量账本守恒。实际修复提交为 `f065d2c`；策略所列 `707f812` 是工作流恢复提交。当前 `HEAD=46ab5ab1bfaf8b4dbe726060db774f4d59d07fc4`，相关产品及复验测试相对修复提交无差异。
- `npm test -- test/combat.test.ts test/entity-vnext.test.ts test/review-regressions.test.ts` 退出 1：加载 Vite 配置时 `esbuild spawn EPERM`，零条测试执行。临时在忽略的 `node_modules/.cache/` 放置进程内 TypeScript 转译 loader 和独立无头脚本；执行 `node --no-warnings --experimental-loader ./node_modules/.cache/daoyan-micro-mana-loader.mjs ./node_modules/.cache/daoyan-micro-mana-reverify.mjs` 退出 0。
- 无头脚本导入减速预设、绑定鼠标、设置法力 400/神识 80、把目标放在 120 距离，逐帧断言 150 次账户精确等式、`manaAccountSnapshot.conserved` 和世界能量账本守恒；第 40 帧出现反噬，共 1 次。第 9/40/150 帧余额分别为 `131272000`、`46775999`、`89015999` 微单位，均守恒。`npm run typecheck` 退出 0。
- [公开缺陷报告](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/bugfix.md)及[结构化复验结果](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/bugfix-reverification.json)标记该缺陷复验通过；标准 Vitest 与本轮浏览器候选交互没有成功运行，旧 QA 和完整门禁只作既有背景。notice guard 负责正式登记及后续节点，不手工推进；本轮未 commit、push、tag 或发布。

## 当前树缺陷复验复核

- 范围与验收沿用 `candidate-micro-mana`：独立核对余额不足反噬、逐帧微单位及世界能量守恒；不改产品代码或游戏测试。当前 `HEAD=9be8b5aa7656e0b252af7b7e008dc57c5c5c8725`，相对实际修复 `f065d2c` 的受影响产品源码、测试和构建配置差异检查退出 0。
- 标准三文件 `npm test -- test/combat.test.ts test/entity-vnext.test.ts test/review-regressions.test.ts` 退出 1，仍在加载 Vite 配置时遭遇 `esbuild spawn EPERM`，零条断言执行。既有进程内转译无头复验命令重新退出 0，150 帧全部精确守恒，第 40 帧首次反噬、总计 1 次；`npm run typecheck` 退出 0。未执行统一门禁或本轮浏览器候选体验。
- [缺陷报告](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/bugfix.md)和[结构化复验](../versions/draft-2026-09-23-7af7515c-4ac5-47fe-9063-/bugfix-reverification.json)已补当前树证据；结论维持通过，Vitest 宿主限制继续明示。未编辑 `.daoyan-agent` 或手工推进版本节点，未 commit、push、tag、发布。

## 缺陷复验门禁循环止损

- `bugfix-2` 和 `bugfix-3` 都已交付同一账目缺陷的通过报告，但版本内核复用旧 Feature 门禁时，把每轮报告与状态文档变更造成的验证树变化当成缺少门禁，连续派到 `bugfix-4`。主 Agent 停止守卫；第 4 轮尚无已完成工作项，其恢复点、前两轮报告和错误对账均保留。
- 缺陷复验改为与版本 QA 共用同源码门禁补证：要求已有修复修订的成功 Feature 完整门禁、当前树的真实成功 `verify:full` 证据，且修复修订至今无产品实现或游戏测试变化。仅这些条件满足才在当前树重新登记 Feature 证据；游戏实现变化继续拒绝复用。空重试可在复验清单和当前树门禁有效时留档作废，复核已交付的上一轮，不再让 Agent 重做同一报告。

## 候选缺陷证据范围修复

- `candidate-micro-mana` 来自候选体验，未绑定单一开发工作项；原复验对账仅从关联工作项取路径，得到空列表，导致合法的版本级验证证据被拒并再次排队。
- 对已关联缺陷仍只记录对应工作项路径；未关联缺陷采用该版本全部工作项的影响路径作为保守的版本级范围，不伪造单一归属。增加两种情况的回归测试；当前产品代码与缺陷复验结果未改。

## 产品意图对齐合入主干

- 将已验证并推送的 `codex/product-intent-alignment` 合入当前主干，保留期间完成的版本归档、秘书候选构建校验和历史开发记录；冲突仅涉及阶段提示及历史文档。新版本策划的意图记录门禁与主干候选构建证据要求均保留。
- 首次合并树完整门禁在 Windows 秘书集成测试清理临时目录时遇 `EBUSY`；481 条断言通过、1 条因清理失败。进程退出与管道文件句柄完全释放之间存在短暂窗口，临时目录清理改用 Node 内置的有界重试，不改变业务运行逻辑。最终验证及 Git 交付以本次合并后的实际命令结果为准。
- 修正后的合并树 `npm run verify:full` 退出 0：482 条单测、覆盖率与沙盒门禁、24 条 Chromium 流程及生产构建全部通过。正式版本运行状态和大版本发布决定未改动。
