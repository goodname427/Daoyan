# 2026-09-27 开发记录

## 主策审核过早等待制作人

- 问题：正式版本 `pilot-agent-intent-2026-09-24` 在 `design-review` 的 `unified-world-review` 任务中，规划器把将来可能需要的产品取舍标为 `producerDecisionRequired=true`。调度器在执行任务前即保存 `waiting-producer` 快照，因而没有产出合同要求的 `design-review-findings.md` 和可审阅选项，制作人无法判断。
- 范围：只修正工作流控制面的正式主策审核任务规划与恢复；游戏世界规则、版本运行状态和已交付策划正文不由主 Agent 修改。非目标是替主策做理论取舍或跳过后续制作人门禁。
- 处理：阶段合同强制将 `design-review` 交付计划的前置 `producerDecisionRequired` 清为 false，保留问题文字供审核使用；主策完成证据后仍由 `design-review.json` 的 `producer-escalation` 提交制作人。规划提示和协作文档同步说明先完成审核材料。既有等待快照经原运行目录续跑，不手工编辑正式版本状态。
- 验收：同一恢复计划继续执行文档任务；其他阶段真正需要先决策的计划保持等待；纯策划只做轻量直接检查。主策产出完整材料后由秘书创建需要的制作人门禁，游戏开发在取舍确认前保持未开始。
