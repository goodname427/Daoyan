# 下一正式版本独立 QA

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 结论：**failed，版本测试未通过。** 本轮原有 6 个文件的 190 条无头断言通过，但标准 Vitest、真实浏览器回归及新构建受当前测试宿主 `spawn EPERM` 阻断。未确认产品行为缺陷，`bugs` 为空；覆盖缺口不能充作产品通过。
- 结构化命令、退出码和证据见 [qa.json](./qa.json)。本轮只记录测试，不修改产品实现或版本运行状态。

## 验收、集成与回归

| 范围                                       | 已执行证据                                       | 尚缺证据                         |
| ------------------------------------------ | ------------------------------------------------ | -------------------------------- |
| 属性驱动统一实体、运动、分池供能与命中守恒 | `entity-vnext` 71 条、`review-regressions` 20 条 | 标准 Vitest 同树复跑             |
| 事件、监控、蓄力、战斗主线                 | `combat` 46 条                                   | Playwright 实际按键和暂停交互    |
| 旧 DSL、旧存档、共享法术书与预设           | `dsl-migration` 18 条、`persistence` 20 条       | 浏览器导入导出和页签往返         |
| 推演台与演武场集成、权限快照、窄屏入口     | `render` 15 条                                   | 窄屏、控制台与真实浏览器视觉体验 |
| 静态与产物                                 | 类型检查、文档检查通过                           | 新生产构建和打包验证             |

标准定向命令在 Vite 配置加载前退出 1；Playwright 在启动时退出 1；`npm run build` 也在配置加载前退出 1，均报 `spawn EPERM`。这些失败没有执行对应断言。进程内替代运行直接使用原测试与源码，关闭配置加载及 esbuild 子进程，190 条断言全部通过；临时 runner 已删除。首次 runner 参数位置错误导致再次加载配置并退出 1，亦完整记录在 JSON。

开发阶段的 `full-gate-evidence.json` 记录一次 `npm run verify:full` 成功，但本轮未确认其候选验证树、配置和命令指纹仍匹配当前树，因此没有复用为本轮 QA 通过事实。后续需在可启动子进程的宿主上完成标准定向测试、相关 Playwright 主线与构建，并核对同树完整门禁证据；由既定阶段流程处理，不手工推进节点。

## 缺陷清单

本轮已执行断言没有确认产品行为缺陷。`spawn EPERM` 是测试宿主限制，记录为验证阻断，不虚构产品缺陷 ID；版本状态仍为 `failed`。
