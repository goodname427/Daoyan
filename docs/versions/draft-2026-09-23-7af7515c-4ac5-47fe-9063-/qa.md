# 下一正式版本独立 QA

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 结论：**failed，版本测试未通过。** 在当前 HEAD `0708ee9` 上重新执行时，类型与文档检查通过；标准 Vitest、Playwright 和生产构建均因测试宿主 `spawn EPERM` 在执行断言、浏览器交互或生成产物前退出。没有确认产品行为缺陷，`bugs` 为空；验证阻断不能充作产品通过。
- 本轮实际命令、退出码和公开证据见 [qa.json](./qa.json)。只执行测试和记录，不修改产品实现或版本运行状态。

## 验收、集成和主线回归

| 范围                                                     | 本轮命令与结果                                                                    | 结论边界                                       |
| -------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------- |
| 验收：统一实体、运动、六属性、分池账本、事件、监控与蓄力 | 定向 Vitest 退出 1                                                                | 配置加载时 `esbuild spawn EPERM`，0 条断言执行 |
| 集成：战斗、旧 DSL/存档迁移、共享法术书、面板权限        | 同一 Vitest 命令退出 1；相关 Playwright 命令退出 1                                | 浏览器未启动，不能确认导入、绑定和实战往返     |
| 回归：旧核心、推演台/演武场、窄屏主线                    | 同一 Vitest 与 Playwright 命令退出 1                                              | 真实交互、控制台与窄屏证据缺失                 |
| 静态与产物                                               | `npm run typecheck`、`node scripts/check-docs.mjs` 退出 0；`npm run build` 退出 1 | 新构建在配置加载时受同一宿主限制               |

更早一轮 QA 在代码修订 `e8ff401` 上以临时进程内 runner 执行了原有 6 个文件的 190 条断言，全部通过。当前 HEAD `0708ee9` 相对上一轮 QA 修订 `1bf0c67` 仅更改 QA 报告、状态文档和开发日志，产品代码与测试未变；该历史结果可作辅助证据，不能当作本轮标准测试或浏览器验收。

开发阶段的 `full-gate-evidence.json` 记录 `npm run verify:full` 成功，本轮没有重跑。该证据绑定候选树指纹 `5f2d47d3…`。此后 `docs/status.md` 与开发日志等受控文件已有变化，旧门禁不能复用为当前树通过。本轮按执行 Agent 范围只做定向验证，不手工推进版本节点。

## 缺陷清单与后续验证

本轮没有执行到能确认产品行为缺陷的标准断言或浏览器流程，故不编造产品缺陷 ID。`spawn EPERM` 是宿主验证阻断，分别记录在命令及证据中。验收范围仍包括运动和法球守恒、探查权限、事件与监控、六属性与两种蓄力；集成范围包括旧 DSL/存档迁移、预设导入、共享法术书和面板；回归范围包括推演台与演武场往返、实战和窄屏。需要在允许启动子进程的测试宿主完成同版定向 Vitest、Playwright 主线与窄屏、生产构建，并由 Feature PM 核对或重建当前候选树完整门禁；本报告不把阶段任务交付视为产品测试通过。
