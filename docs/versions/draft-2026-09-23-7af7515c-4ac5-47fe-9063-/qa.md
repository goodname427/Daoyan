# 下一正式版本独立 QA

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 基线：`65b33ac9bce739268348868ecb4a7a9ef61b648b`；开测前工作区干净。相对上轮 QA 基线 `0708ee9`，仅 QA、状态和开发日志变化，产品源码、测试和构建配置未变。
- 结论：**failed，版本测试未通过。** 本轮 7 组原有无头测试共 219 条通过，类型与文档检查通过；标准 Vitest、Playwright 和构建仍因宿主 `spawn EPERM` 在断言、浏览器或产物阶段前退出。未确认产品行为缺陷，`bugs` 为空。任务交付不等于产品测试通过。
- 实际命令、退出码和公开证据见 [qa.json](./qa.json)。本轮只执行测试和记录，未修改产品实现或版本运行状态。

## 验收、集成与主线回归

| 范围                                                     | 本轮证据                                                                                     | 结论边界                                                          |
| -------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 验收：统一实体、运动、六属性、分池账本、事件、监控与蓄力 | 原 `entity-vnext`、`core`、`combat`、`review-regressions` 测试由临时进程内 Vitest 执行并通过 | 这是无头行为证据；标准 Vitest 在配置加载时退出 1，未执行断言      |
| 集成：战斗、旧 DSL/存档迁移、共享法术书、面板权限        | 原 `dsl-migration`、`persistence`、`render` 测试由同一 runner 执行并通过                     | jsdom 渲染不等于真实浏览器的导入、绑定及实战往返                  |
| 回归：旧核心、推演台/演武场及窄屏主线                    | 上述 7 文件合计 219 条通过；Playwright 退出 1                                                | Playwright 未启动，0 条浏览器用例；真实交互、控制台和窄屏证据缺失 |
| 静态与产物                                               | `npm run typecheck` 与 `node scripts/check-docs.mjs` 通过；`npm run build` 退出 1            | 本轮没有新构建或打包证据，现有 `dist/` 未核对为当前版本产物       |

临时进程内 runner 用 `startVitest`、`configFile: false`、`esbuild: false`、单线程和 TypeScript `transpileModule` 执行**原有测试文件**，不改正式测试或 Vite 配置，运行后删除。7 文件的通过数分别为 `entity-vnext` 71、`combat` 46、`core` 29、`review-regressions` 20、`dsl-migration` 18、`render` 15、`persistence` 20。它能确认这些断言在当前源码上通过，但不能替代标准测试门禁或浏览器验收。

开发阶段旧 `verify:full` 成功证据绑定候选验证树指纹 `5f2d47d3…`，后续受控文档已变化；本轮不复用为当前树完整门禁通过，也不重跑 Feature PM 负责的统一门禁。浏览器和新构建无法在本宿主取得通过证据，因此当前版本保持 `failed`。

## 缺陷清单与补验

已执行的 219 条断言没有确认产品行为缺陷，不编造缺陷 ID。`spawn EPERM` 作为宿主验证阻断记录在命令和证据中，`bugs` 保持空数组。仍需在可启动子进程的宿主完成当前版标准定向 Vitest、Playwright 主线与窄屏、生产构建/候选验证；Feature PM 负责匹配或重建当前候选树的完整门禁证据。版本节点由 notice guard 对账，不由 QA 手工推进。
