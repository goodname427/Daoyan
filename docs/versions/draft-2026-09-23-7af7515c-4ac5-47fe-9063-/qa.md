# 下一正式版本独立 QA

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 本轮基线：`c725e334fdebc09dfd7012ad971250841dfb4b95`；开工时 Git 工作区干净。
- 结论：**blocked**。本轮未确认新的产品行为缺陷，`bugs=[]`。类型检查通过，但验收/集成/回归断言、浏览器流程及生产构建均因当前执行环境的 `spawn EPERM` 未运行。完整命令和退出码见 [qa.json](./qa.json)。

## 范围与实际结果

| 套件      | 原定核对路径                                                 | 本轮事实                                                                                                        |
| --------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| 验收      | 统一实体、运动/供能、权限和六属性、账本、事件/监控、两种蓄力 | 7 文件定向 Vitest 在 Vite 配置加载时退出 1，零断言；`test/combat.test.ts` 新增的 `0.016` 秒账本守恒回归未执行。 |
| 集成      | Battle、旧 DSL/存档迁移、预设、共享法术书与面板              | 同一 Vitest 命令未进入断言；Playwright 在创建 worker 时退出 1。                                                 |
| 回归      | 核心流程、推演台与演武场往返、窄屏和迁移                     | Playwright 打印“Running 20 tests”后因 `spawn EPERM` 退出，没有浏览器用例结果；`npm run typecheck` 退出 0。      |
| 构建/体验 | 当前源码生产构建与页面操作                                   | `npm run build` 在 Vite 配置加载时退出 1，本轮没有新产物或实际页面体验。                                        |

先前报告的 219 条标准断言、20 条 Chromium 流程和构建通过属于账本修复 `f065d2c` 之前的源码，不能复用为本轮当前修订的独立 QA。此前候选实测发现的 1 微单位法力账目差异已有修复和回归用例，但该用例本轮未能执行；这里既不宣称修复已获独立复验，也不凭启动失败登记产品 Bug。

## 恢复条件

在允许 esbuild 与 Playwright 子进程启动的宿主，对当前修订重跑 [qa.json](./qa.json) 中失败的定向测试、浏览器用例和生产构建，并按验收合同核对迁移、资源守恒、两种蓄力及推演台/演武场往返。若发现产品行为失败，应以稳定 ID、预期/实际、证据及工作项关联完整登记缺陷并改为 `failed`；若全部实际通过，再更新当前树的 QA 结论。正式节点和 Feature 完整门禁由 notice guard 对账，本报告没有手工推进运行状态。
