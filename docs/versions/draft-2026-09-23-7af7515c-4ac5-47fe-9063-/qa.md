# 下一正式版本独立 QA 补验

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 结论：当前游戏源码的验收、集成与回归补验通过，未确认新增产品缺陷；正式 QA 节点尚待 notice guard 核对同树完整门禁后接纳。

独立 QA Agent 的隔离宿主无法启动 esbuild 和 Playwright worker，原始 `blocked` 报告、命令失败及审查仍保留在 `qa-8` 运行目录。主 Agent 未修改游戏源码或游戏测试，在可运行宿主执行同一份[QA 清单](./qa.json)中的失败命令，得到 7 组 220 条定向测试、20 条 Chromium 流程、类型检查和生产构建全部通过。定向测试包含减速预设以 `0.016` 秒步长运行 150 帧、每帧检查法力账户守恒的回归。

本结论只补齐本轮 QA 的可执行证据，不复用修复前的旧 QA，也不代替候选构建的真实页面体验或制作人候选验收。候选实测中发现的账目缺陷仍须在正式流程中完成复验登记；若后续发现行为失败，应登记稳定缺陷 ID、预期、实际和证据。
