# 下一正式版本独立 QA

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 原 QA 基线：`65b33ac9bce739268348868ecb4a7a9ef61b648b`。四轮执行 Agent 沙盒尝试均因 `spawn EPERM` 无法完成标准测试、浏览器及构建，失败报告保留于 Git 历史和阶段运行目录。
- 宿主补验结论：**passed，未确认产品行为缺陷。** 主 Agent 在可运行宿主对相同游戏源码运行 7 文件 219 条标准断言、20 条 Chromium 浏览器流程、类型检查和生产构建，均通过。`bugs` 为空；这不抹去原沙盒失败事实，也不代替当前树的完整 Feature 门禁对账。
- 宿主补验的实际命令、退出码和公开证据见 [qa.json](./qa.json)。没有修改产品实现或手工推进版本运行状态。

## 验收、集成与主线回归

| 范围                                                     | 本轮证据                                                            | 结论边界                         |
| -------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------- |
| 验收：统一实体、运动、六属性、分池账本、事件、监控与蓄力 | `entity-vnext`、`core`、`combat`、`review-regressions` 标准测试通过 | 无头行为证据，不单独代表真实交互 |
| 集成：战斗、旧 DSL/存档迁移、共享法术书、面板权限        | `dsl-migration`、`persistence`、`render` 标准测试通过               | 与浏览器结果共同核验             |
| 回归：旧核心、推演台/演武场及窄屏主线                    | 7 文件 219 条标准断言；20 条 Chromium 浏览器流程通过                | 包含窄屏和往返主线               |
| 静态与产物                                               | `npm run typecheck` 与 `npm run build` 通过                         | 产物来自本轮构建                 |

原 Agent 沙盒的临时进程内 runner 通过 219 条原有断言，但不能替代标准测试或浏览器验收。宿主补验重新使用标准 `npm test -- ...`；7 文件的通过数分别为 `entity-vnext` 71、`combat` 46、`core` 29、`review-regressions` 20、`dsl-migration` 18、`render` 15、`persistence` 20。

原 Agent 沙盒四轮的 `spawn EPERM`、失败退出码与无产品缺陷的判断保留在各轮报告，不伪造缺陷 ID。当前候选验证树需重新匹配完整门禁证据后，秘书才能接纳本报告并推进节点。

## 缺陷清单与补验

本次宿主补验没有确认产品行为缺陷，`bugs` 保持空数组。原沙盒环境阻断不是产品 Bug，不进入版本缺陷修复环节。当前树完整门禁和正式版本阶段仍由 notice guard 对账，不由 QA 手工推进。
