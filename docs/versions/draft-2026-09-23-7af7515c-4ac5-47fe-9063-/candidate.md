# 属性驱动统一实体与资源守恒法术：候选构建

## 当前本机补验（2026-09-24）

- 先前隔离 Agent 的 `spawn EPERM` 与浏览器策略错误仍作为原始阻断证据保留，以下结果来自可运行的本机宿主，不追改旧运行结论。
- 本机以 `b791f53d634a5ac136dd166c98fa4bddbfbf925e` 为构建源码，生产构建退出 0；从新 `dist/` 启动预览后，预设导入、推演、演武绑定与施放、法术书往返、390px 窄屏及低法力账本三条 Chromium 流程均通过。产物 SHA-256、执行时间和浏览器结果见[candidate.json](./candidate.json)与[candidate-browser.json](./candidate-browser.json)。
- 这只证明候选构建与真实页面路径可体验；正式候选节点已通过受审计接口接纳，当前进入制作人候选体验门禁。本节不代表版本已归档或对外发布。

## 隔离 Agent 候选核对（2026-09-24，已由本机补验接续）

- **阶段结论：受宿主环境阻断，尚不能交付供制作人验收的已验证候选构建。** 当前 `HEAD=b791f53d634a5ac136dd166c98fa4bddbfbf925e`，开工时工作区干净；本轮未改产品实现、游戏测试或构建配置。既有 QA 的产品基线 `0515b4b7c0357cc37300c6dff1d027c1410d5024` 至当前修订，`src`、`e2e`、三项相关游戏测试、包清单、Vite 配置与 Electron 目录的 `git diff --quiet` 退出 0。已有[独立 QA](./qa.md)在该产品基线记录 220 条定向测试、20 条 Chromium 流程、类型检查与生产构建通过；[账目缺陷复验](./bugfix.md)记录 150 帧逐帧守恒。它们是同源码回归背景，不是本轮候选页面体验。
- 本轮 `npm run typecheck` 退出 0。`npm run build` 退出 1，Vite 加载配置时 `esbuild spawn EPERM`，**没有新候选产物**。现存 `dist/` 的三项文件与哈希列在下表，仅供恢复时识别；当前宿主无法以新构建证明它们与已测修订精确对应。
- `python -m http.server 4189 --bind 127.0.0.1 --directory dist` 可提供静态入口；本机 HTTP 请求返回 200。浏览器控制先后在枚举和打开 Edge 候选页时返回 `Unable to load browser request-header policy`，重试后仍失败；本轮没有浏览器页面操作、窄屏视觉检查或控制台观察。HTTP 200 不等于候选体验通过。

| 现存文件                         | SHA-256                                                            |
| -------------------------------- | ------------------------------------------------------------------ |
| `dist/index.html`                | `28E97A147139C51DCA1D23A28962DBEBDB0B11921D22622CE55AB3905AD08438` |
| `dist/assets/index-BthUe770.css` | `E7083B13CAD42CD228F63FBA2BB6FA08CB2FECF103D4EF89D9552603F4C093B3` |
| `dist/assets/index-D8V7kwuL.js`  | `2421CC9A0E2D3AF293356BA7B669B32D96B4FB44826173CFDAEA9F3706218F8B` |

**恢复条件：**在能启动 `esbuild` 且可访问本机候选页的宿主，对相同产品源码运行 `npm run build`，记录退出码、修订及新产物哈希；从该产物完成预设导入、推演、演武绑定与实战、法术书往返，复查法力不足反噬后的账本状态、窄屏布局与控制台。只有这些证据与已测代码修订一致，才能提交候选体验给制作人；此前缺陷和旧候选体验不能替代本轮验收。此处没有修改 `.daoyan-agent`、手工推进节点或视为制作人批准。

- 版本：`draft-2026-09-23-7af7515c-4ac5-47fe-9063-`
- 核对日期：2026-09-23
- 当前修订：`b5aa46cd8779552d52a40f000ca4c8f38cb4d33a`
- 阶段结论：隔离 Agent 宿主不能重新构建或打开浏览器；随后主 Agent 在可运行宿主完成了同源码生产构建且产物哈希一致，但此前候选页面访问被策略拒绝，**候选真实体验尚未完成，不能登记候选阶段通过**。制作人候选验收和发布均未进行。

## 体验入口

在允许启动本机服务的宿主，于项目根目录运行 `npm run preview -- --host 127.0.0.1 --port 4173`，打开 <http://127.0.0.1:4173/>。静态文件位于 `dist/`；换机后须先在可构建宿主运行 `npm run build`。本阶段没有安装包。

建议在推演台查找六项新增属性控制和逐字段探查，选择性导入“对手减速”“对手破防”，推演后切到演武场绑定并实战，再返回推演台核对法术书。另可体验冲量滑行、持续推进及追踪法球，观察分池余额耗尽后主动行为停止；检查受击等被动事件、付费主动监控和两种蓄力会话。具体语义与限制见[实体与属性说明](../../reference/entities-and-attributes.md)。

## 版本说明

- Actor 与 Projectile 共用实体句柄及能力判定；运动状态分开记录实际速度向量和速度上限，角色驱动、法球推进与追踪有来源和资源结算，惯性滑行不逐帧收费。
- 法球创建、注能、扫描、运动和命中伤害经分池账本结算；伤害受已付储能约束，余额不足时停止主动推进或追踪。
- 六项属性控制覆盖生命上限、法力上限、法力回复、神识上限、施法速度及法力消耗倍率；当前生命、法力与占用遵循各自的结算通路，自身增益有正价与防套利边界。
- 逐字段探查和属性面板遵守能力、权限与感知条件；离散被动事件可绑定多个响应，主动监控须显式开启并付费。输入意图可驱动蓄时后瞬发或持球注能后发射。
- 推演台提供控制入口和可选择导入的减速、破防预设；旧 DSL 与存档按已定义的兼容或拒绝诊断处理，不覆盖现有法术书。

## 代码、测试与产物对账

独立[版本 QA](./qa.md)在游戏源码基线 `65b33ac9bce739268348868ecb4a7a9ef61b648b` 上补验 7 个标准测试文件的 219 条断言、20 条 Chromium 浏览器流程、类型检查和生产构建，均记录为退出 0，未确认产品行为缺陷。当前修订相对该基线的 `src/`、`e2e/`、`package.json`、`package-lock.json`、`vite.config.ts`、`electron/` 无差异（本轮 `git diff --exit-code` 退出 0）。这说明当前游戏源码仍是 QA 检验的源码；QA 报告同时注明完整 Feature 门禁的候选验证树仍须由 notice guard 核对。

当前 `dist/` 文件时间为本日 21:05，包含本版的“读取神识占用”“注入运动能量”“设置法球追踪”“对手减速”“对手破防”文本。文件 SHA-256 如下，可用于接管宿主逐项复核：

| 文件                             | SHA-256                                                            |
| -------------------------------- | ------------------------------------------------------------------ |
| `dist/index.html`                | `0773471E7FB680D76FA0323EBA2FFCE94391A4F91903FF73C958F2193310AF2F` |
| `dist/assets/index-BthUe770.css` | `E7083B13CAD42CD228F63FBA2BB6FA08CB2FECF103D4EF89D9552603F4C093B3` |
| `dist/assets/index-jVGlqQ2h.js`  | `472392E145733204C6D59330887AD3D27217D16B105B532031F08C7D8CA74011` |

本轮 `npm run build` 退出 1：Vite 加载配置时 `esbuild spawn EPERM`，未生成新产物；现有文件不能据此记作**本轮新构建**。尝试用 Python 在 `127.0.0.1:4173` 提供静态文件，绑定端口因 `WinError 10013` 失败；Computer Use 浏览器入口不可用。因此本轮没有 HTTP 或浏览器交互验收。上述哈希仅证明候选文件可识别，不能单独证明其精确构建来源；可运行宿主应重新构建并体验，再将实际产物哈希与通过的代码修订登记为最终候选证据。

## 测试结论与遗留风险

- QA 已通过的标准测试、浏览器主线、窄屏和迁移流程适用于未变化的游戏源码；本轮没有新增产品实现，也没有发现新产品缺陷。
- 候选文件与本轮可运行源码的构建对应关系尚缺重新构建的直接证据；真实候选入口操作、控制台错误和布局检查尚未在本轮完成。这两项是候选阶段完成前必须补齐的证据。
- 旧读取语义不能无损迁移的法术会保留原文并给诊断；法球没有免费回蓝或意识，`dist/` 是本机忽略 Git 的静态产物，换机不能凭仓库检出直接体验。

本报告仅保存公开恢复点；不修改 `.daoyan-agent` 状态，不手工推进版本节点，不表示制作人已批准或候选已归档。

## 可运行宿主补验（2026-09-23）

主 Agent 在同一工作区的可运行宿主执行 `npm run build`，退出 0，Vite 转换 237 个模块。`git diff --quiet 65b33ac9bce739268348868ecb4a7a9ef61b648b -- src e2e package.json package-lock.json vite.config.ts electron` 退出 0；当前游戏源码和构建配置仍与版本 QA 基线一致。重新生成的三个 `dist/` 文件 SHA-256 与上表逐一相同，补齐了该源码在可运行宿主可重现构建的证据。`npm run verify:full` 在此宿主退出 0，包括 467 条标准测试、覆盖率、沙盒、20 条 Chromium 流程和生产构建。

上述浏览器测试运行于自动化开发服务，不代替从候选 `dist/` 入口的真实体验。先前候选页面访问被浏览器策略拒绝，主 Agent 未绕过；因此推演台、演武场、窄屏和控制台的候选页面验收仍待完成。本节只关闭构建来源疑问，不批准候选阶段、制作人验收或归档。

## 独立审查修复复核（2026-09-23）

审查指出候选产物缺少与 QA 修订对应的新构建和真实体验证据，结论成立。当前工作区 `HEAD=b5aa46cd8779552d52a40f000ca4c8f38cb4d33a`；`git diff -- src e2e package.json package-lock.json vite.config.ts electron` 无输出。再次运行 `npm run build` 退出 1，Vite 加载配置时 `esbuild` 子进程 `spawn EPERM`；运行 `npm run preview -- --host 127.0.0.1 --port 4173` 同样退出 1，未打开候选页面。直接 Node HTTP 监听 `127.0.0.1:4188` 成功，随后已停止；这仅排除所有本机端口均不可绑定的推断，不构成 Vite 预览或游戏体验。`4173` 当前已被占用，接管时可使用其他端口。

本轮没有新增构建产物，前述 `dist/` 哈希仍只有文件识别价值。**独立审查 high finding 未关闭，候选阶段继续阻断。**恢复时须在允许 `esbuild` 启动的宿主以相同游戏源码运行生产构建，记录命令、退出码、修订和产物 SHA-256；从该产物启动预览，实际走通推演台导入、推演、演武场绑定与战斗、返回法术书，并检查控制台及窄屏布局。验证完成前不得把旧 `dist/` 当作已验候选，也不得登记候选阶段通过。

## 本次修复复核（2026-09-23）

增量复审再次确认宿主子进程限制：`npm run build` 退出 1，`node` 启动另一个 `node -v` 子进程同样返回 `spawnSync ... EPERM`。`git diff --exit-code 65b33ac9bce739268348868ecb4a7a9ef61b648b -- src e2e package.json package-lock.json vite.config.ts electron` 退出 0。Computer Use 浏览器枚举仍返回 `Unable to load browser request-header policy`。本次未生成新构建或获得候选页面体验证据，下面原有阻断结论继续适用。

再次在 `b5aa46cd8779552d52a40f000ca4c8f38cb4d33a` 运行 `npm run build`，退出 1，Vite 加载配置时 `esbuild spawn EPERM`，没有生成新产物。`npm run preview -- --host 127.0.0.1 --port 4193` 同样退出 1。浏览器控制入口返回 `Unable to load browser request-header policy`，无可操作浏览器；因此没有从候选页面完成推演台、演武场或窄屏体验。本次没有产品代码或测试改动，不能以既有 `dist/` 哈希代替新构建来源。审查 high finding 仍开放，候选阶段仍阻断；恢复条件与上段一致。

## 本轮独立审查修复尝试（2026-09-23）

在同一 `HEAD=b5aa46cd8779552d52a40f000ca4c8f38cb4d33a` 再次执行 `npm run build`，退出 1，Vite 配置加载时 `esbuild spawn EPERM`，没有得到与当前修订对应的新候选产物。浏览器枚举重试后识别到 Edge；本机静态预览服务在 `127.0.0.1:4193` 启动，但打开该地址时浏览器安全策略明确拒绝访问，理由为用户拒绝该请求，并要求不得通过替代浏览器或间接方式绕过。预览服务随后停止。没有发生页面操作，推演台、演武场、窄屏布局和控制台均未获得本轮体验证据。

审查 high finding **仍未关闭**，候选阶段不能登记通过。恢复需要在允许 `esbuild` 子进程及候选页面访问的宿主重新运行生产构建，并从该构建的预览页面执行上文的完整体验路径；产物哈希、命令结果、代码修订及页面观察须一并登记。本轮未修改产品代码或测试，也未手工推进版本状态。

## 本次独立审查修复结果（2026-09-23）

当前 `HEAD=b5aa46cd8779552d52a40f000ca4c8f38cb4d33a`。`git diff --exit-code 65b33ac9bce739268348868ecb4a7a9ef61b648b -- src e2e package.json package-lock.json vite.config.ts electron` 退出 0，游戏源码及构建配置与已测 QA 基线一致；`npm run typecheck` 退出 0。`npm run build` 退出 1，仍在加载 Vite 配置时遇 `esbuild spawn EPERM`，本次未生成候选产物。此前浏览器安全策略已明确拒绝预览页面访问，本次未尝试其他入口，也未取得真实页面体验。

因此产物与当前源码的一次构建对账、推演台和演武场操作、窄屏布局及控制台检查仍缺失。审查 high finding 继续开放，候选阶段不能登记通过；恢复所需命令与体验路径见上文。本次没有产品代码或测试修改，没有改动运行状态或手工推进版本节点。

## 本轮修复 Agent 复核（2026-09-23）

再次运行 `npm run build`，退出 1：Vite 加载配置时 `esbuild spawn EPERM`。本轮没有新候选产物，既有 `dist/` 哈希仍不能证明与已测源码对应。此前浏览器安全策略明确拒绝候选地址访问，本轮未绕过该拒绝，因此没有推演台、演武场、窄屏布局或控制台的真实页面观察。审查 high finding 保持开放，候选阶段不得登记通过；恢复仍须由允许构建和访问候选页面的宿主完成上文所列的同源码构建对账及交互体验。

## 主 Agent 本机候选体验补验（2026-09-23）

在 `HEAD=2e51cd80dfb233514cdce522cee2594d741126d3` 的可运行宿主重新执行 `npm run build`，退出 0，产物三项 SHA-256 与上表完全一致。候选预览由 `npm run preview -- --host 127.0.0.1 --port 4173 --strictPort` 提供；浏览器控制服务恢复并经制作人重新授权后，已从该地址刷新页面并实际操作，不再把旧页面或开发服务当作候选体验。

- 推演台显示 94 项元法术，可选择导入“对手减速”和“对手破防”，法术书由 14 项增至 16 项，语法解析通过。减速预设按说明调整为神识 80、法力 400 后推演成功，实扣法力 323、峰值神识 71；默认神识 64 和法力 300 不足，属于预设文案应更显眼的体验风险。
- 演武场显示新导入法术并可绑定左键，调整基础属性后开始战斗；神识 80、法力 800 时减速施放完成且账户摘要为“守恒”。切回推演台，导入的两项法术仍在法术书。浏览器控制台未记录错误。390px 视口下 DOM 宽度与文档滚动宽度均为 390px；仅此不能证明所有窄屏视觉细节合格。
- **发现待修缺陷**：在演武场神识 80、法力 400 下，减速施放因余额不足反噬后，账户摘要持续显示“账目待核对”。无头复现把敌人移到可扫描距离，并以 `0.016` 秒步长推进 9 次，`manaAccountSnapshot` 的入账与出账相差 1 微单位，`conserved=false`。这是精度对账问题，不应在能量守恒版本作为已通过候选；须修复并补足失败施法与非整秒帧长的回归测试，再重跑受影响门禁和候选体验。

本节是公开体验与缺陷证据，不直接编辑运行状态，不代表候选阶段、制作人验收或归档通过。

## 主 Agent 精度修复补证（2026-09-23）

给上述 `0.016` 秒减速场景新增逐帧法力账户守恒回归，修复前稳定失败。账本现在将浮点投影误差范围内的百万分之一单位还原到整数计量；回归通过，`npm run verify:full` 退出 0（469 条单测、20 条浏览器流程和生产构建）。本次修复改变游戏核心源码，先前候选构建哈希及 QA 同源码结论不再适用；须在当前代码修订重新做独立审查、候选交互和版本门禁，未记录候选通过或制作人批准。
