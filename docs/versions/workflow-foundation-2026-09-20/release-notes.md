# 候选版本说明

这次交付把“版本”从一组连续开发任务，升级为有策划、评审、开发、测试、体验和归档的正式生命周期。

制作人现在可以在“道衍项目中枢”里看到当前版本处于哪个环节、每个节点由谁负责、节点产物和结论、开发任务、缺陷以及自己的待办。页面底部可以直接与常驻秘书交流，不需要判断消息属于新任务、问题还是评审回复。

日常只需要运行：

```powershell
npm run secretary:open
```

本轮没有修改游戏玩法。下一阶段会在这套基础上接入主策、策划、Version PM 与测试 Agent 的自动阶段调度，让正式版本不只“看得见”，也能按流程自行持续推进。
