# 执行计划

## 范围

1. 建立正式版本状态模型与迁移种子。
2. 扩展 notice guard 的只读状态和安全文档 API。
3. 实现可视化秘书页面与自然语言收件。
4. 补齐单元、接口和浏览器体验验证。
5. 更新工作流、状态和使用文档。

## 依赖

- 复用 `.daoyan-agent/secretary/state.json` 的对话、队列和任务完成记录。
- 复用 notice guard 的 `/intake` 收件能力。
- 旧 `version-dispatcher` 仅作为未来开发阶段的批次执行器。

## 范围冻结

本版本不加入游戏功能开发；当前旧元法术迁移任务保留在既有队列，不作为本版本验收内容。
