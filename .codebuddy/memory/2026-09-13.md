# 2026-09-13

## 稳定迭代工作流升级

- 建立 `AGENTS.md` 作为新协作者和 AI 的统一入口。
- 将当前状态、产品规则、架构不变量、测试策略、功能规格和开发日志分层，避免长期事实散落在日期日志中。
- 工作流增加 Definition of Ready、Definition of Done、真实体验验收和文档同步要求。
- 本地 `verify:full`、CI 和推送前门禁使用同一条完整验证链路。
- CI 主分支从错误的 `main` 对齐为实际的 `master`，并允许 `v*` tag 触发 Windows 打包。
- 新增文档链接和版本一致性检查、PR/Issue 模板与 Dependabot。

## 发现并修复的不一致

- `package.json`/lockfile 的 0.1.0 与 CHANGELOG 的 0.2.0 不一致，统一为 0.2.0。
- README、路线图和旧长期记忆仍描述阶段一或独立蓝图页签，现已改成当前产品事实。
- 仓库尚无版本 tag；发布脚本以阶段四完成提交 `e4e71fe` 作为 0.2.0 历史基线，直到下一个正式 tag 建立。
- GitHub 上的 `master` 分支保护需要人工开启，CI 中的 `verify` 应设为必需检查。

## 当前玩法文档基线

- 新增玩家法术编写指南，覆盖类型、语句、生命周期、常用范例、资源面板、失败排查和能力边界。
- 元法术参考从注册代码自动生成并纳入 `docs:check`，当前记录 59 个元法术和 6 个实现分组。
- 新增 Actor、Projectile、属性与真实资源公式的现状文档。
- 元法术五大类、动态计价、Projectile 统一实体化和移除冷却已记录为 vNext 讨论稿，尚未采纳或实现。

## 并发施法与 Git 约定

- 不同槽位现在可以同时运行多个施法实例；同一槽位不可重入。
- 并发 VM 独立推进 tick，实时共享 Actor 的当前法力和神识，受击会打断全部可打断实例。
- 修复 `repeat` 数字计数器被 VM 当作假值、导致循环不执行的问题。
- 用户明确授权 AI 在逻辑版本完成并验证后主动形成 Git 提交，并自行决定推送和 tag；只有大版本发布需要用户决定。
