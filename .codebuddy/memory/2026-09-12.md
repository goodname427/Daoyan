# 2026-09-12

## 项目启动：道衍（Noita -like 修仙编程 roguelike）

### 用户确认的关键决策
- **技术栈**：TypeScript（AI 友好 + 可无头测试）；不用 Unity/Godot 等重型引擎；
  渲染 Canvas2D → 后期 PixiJS；节点编辑 React Flow；最终 Electron 打包 exe（后期可换 Tauri 减小体积）
- **输入方式**：可视化节点图为主，但先用类代码 DSL 验证原型
- **图灵完备性**：默认禁止递归（DAG 保证消耗可静态精确分析）；
  高阶「轮回术」解锁递归 + fuel + 走火入魔

### 资源模型（核心设计）
- 神识 = 持有状态的成本（内存/空间）
- 法力 = 与外界交互的成本（I/O）
- 耗时 = 执行步数（时间）
- 规则：神识内运算（加减/比较/距离/长度）几乎不要法力；
  读写世界的元函数收高额法力 → 自然涌现「批量 I/O vs 逐个 I/O」的优化权衡

### 架构决定
- **AST 是唯一真相且可 JSON 序列化** → DSL 与未来节点图产出同一份结构；AST 即秘籍分享码
- 静态分析在 AST 上做（不在字节码上），才能算准神识峰值与分支最坏情况

### 已完成（阶段一：无头核心）
- `src/core/`：types / ast / meta / builtins / analyzer / compiler / vm / world / dsl
- 受限 VM：DECL/FREE 管神识、CALLMETA 收法力、IDX 与元函数收耗时、fuel 兜底
- VM 内置 `trace`（为后续推演/单步调试模式预留）
- 8 个单元测试通过；`npm run dev` 沙盒验证三维度取舍成立

### 踩过的坑
- `for` 循环用 PEEK 复制栈顶元素会残留副本 → 加 `SWAP` 指令修正栈布局
- `for`/`repeat` 的退出跳转必须回填地址，否则默认跳到 0 号指令
- DSL tokenizer 需要把 `->` 作为整体 token（函数返回类型）
- 分析器的 `return` 语句必须分析其表达式，否则消耗漏算

---

## 阶段二：协作工作流搭建（完成）

### 用户要求的工作流闭环
提想法 → 我实现 → 自动验证（verify）→ 打包/一键启动供体验 → 反馈。
用户明确要求把 git、改动留痕、文档留痕、测试框架等基础设施一次搭全。

### 已落地
- **git**：已 init；`.githooks/`（pre-commit 跑 `npm run verify`，commit-msg 校验 Conventional Commits）；
  `npm run hooks` 设置 core.hooksPath（不用 husky，避免 install 脚本被拦截）
- **质量门禁**：ESLint 9 flat config + Prettier + Vitest（core 覆盖率阈值 70/60/70/70）；
  `npm run verify` = typecheck + lint + format:check + test
- **留痕**：`docs/adr/` × 5（资源模型 / 禁递归 / 技术栈 / AST 为唯一 IR / 列表定容）；
  `docs/dev/YYYY-MM-DD.md` 开发日志；`CHANGELOG.md`；`scripts/release.mjs` 自动发版打 tag；
  `.github/workflows/ci.yml`（push 跑 verify，打 tag 出 exe 产物）
- **体验层**：`src/app/` React 推演台（法术列表 / 静态消耗卡 / DSL 编辑器 / 推演滑杆与实测 / 战场 Canvas / 元函数定价表）
- **打包**：Electron 33 + electron-builder 25；`npm run dist` 产出免安装版 + NSIS 安装程序（各约 71MB）

### 命令约定（重要，后续沿用）
- `npm start` = `vite --open`（一键体验，浏览器）
- `npm run desktop` = `scripts/desktop.mjs`（Vite + Electron 窗口）
- `npm run dist` = `vite build` + electron-builder
- `npm run sandbox` = 无头 CLI 沙盒（原来的 `npm run dev` 已改为 vite）

### 打包踩坑
- Electron 的 postinstall 被 allow-scripts 拦截，但二进制实际已存在（optional deps），`npx electron --version` 可用
- **winCodeSign 解压需要创建符号链接**，普通权限 Windows 会失败 →
  解决：`build.win.signAndEditExecutable = false` 跳过签名。需要正式签名时再开开发者模式
- 偶发 "output file is locked"（杀软/残留进程），重试即可

---

## 推送到 GitHub（完成）

- 首次提交 `dc13c7d chore: 初始化道衍仓库与协作工作流` 已推送到
  `https://github.com/goodname427/Daoyan`（分支 `master`，已设 upstream）
- `git remote add origin` 之前仓库没有任何 remote
- **代理坑**：全局 git 代理 `socks5://127.0.0.1:7890` 已失效（端口无监听），
  且直连 GitHub 会被 reset。实际可用的是 **7897** 端口（HTTP 混合代理）。
  推送命令：`git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push`
- 未修改全局 git config（安全约定），每次需要外网 git 操作时按上面方式临时覆盖
